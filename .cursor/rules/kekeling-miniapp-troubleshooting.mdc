---
description: 科科灵小程序故障排查指南。调试小程序行为异常、支付状态、接单问题、就诊人、客服消息、主题图标等问题时自动应用。
globs: miniapp-shell/**, src/components/terminal-preview/**
alwaysApply: false
---

# 科科灵 小程序故障排查

小程序行为异常时，按正确顺序检查链路，而非先追 UI 表象。

## 通用分诊顺序

1. 确认问题属于: wrapper 导航 / 共享页面逻辑 / token 会话 / 后端状态 / 小程序渲染或宿主能力
2. 确认受影响角色: 用户 / 陪诊员 / 客服
3. 确认是否: 仅小程序 / Web 预览也有 / 后台也有
4. **如果 Web 预览和后台也显示错误状态 → 非小程序专属 Bug**

## 支付状态错误

症状: 用户支付成功，小程序/后台仍显示未支付，无法继续派单

检查顺序:
1. 支付回调是否到达后端
2. 后端订单状态是否更新
3. 小程序页面是否读取正确订单接口（非缓存数据）
4. 支付后导航是否跳转到正确订单页
5. 后台和小程序的状态映射是否一致

**关键**: 如后台也显示未支付 → 根因通常是后端回调/状态流转，非小程序 wrapper

## 陪诊员无法接单/工作台不可用

症状: 打开工作台/订单池时意外弹出登录、无法进入订单池、无法接单

相关文件: `packageC/pages/workbench/index.tsx`, `packageC/pages/orders-pool/index.tsx`

检查顺序:
1. `escortToken` 是否存在
2. `useViewerRole` 是否验证成功
3. dev-mode 自动登录是否返回真实 token
4. 页面路由是否指向实际 packageC 路径
5. 后端订单池/接单接口是否返回角色或状态错误

**关键**: "无法接单" 的实际原因可能是 token 无效 / 角色降级 / 订单状态不符 / 请求通道错误

## 派单失败

症状: 订单存在但后台无法派单

小程序角度: 通常非小程序根因，而是与支付、订单分配或后端校验相关的状态链问题
检查: 后台是否看到同样的阻塞状态 → 订单状态在小程序进入流程前是否已不一致

## 就诊人增删改问题

症状: 添加成功但页面未刷新、删除无效、列表过时

相关文件: `packageB/pages/patients/index.tsx`, `packageB/pages/patient-edit/index.tsx`

检查顺序:
1. 保存/删除 API 是否真正成功
2. 编辑页返回行为是否正确
3. 就诊人页 `useDidShow` 刷新是否触发
4. 共享页面是否响应 refreshTrigger
5. 路由回退是否意外返回错误层级

**已知模式**: 项目已使用 `useDidShow` + `refreshTrigger`，Bug 持续时先检查共享页面保存回调

## 客服消息问题

症状: 无新消息提示、消息列表过时、会话意外关闭

相关文件: `packageB/pages/customer-service/index.tsx`
当前实现: 3 秒轮询刷新，页面隐藏时停止轮询

检查顺序:
1. 会话是否创建成功
2. 页面可见时轮询是否运行
3. 新消息是否与缓存的最后消息 ID 不同
4. 后端会话状态是否变为 closed
5. "缺少提示" 是产品缺口还是交付故障

**重要**: "没有消息声音/弹窗" 当前更可能是能力缺失（需求）而非渲染 Bug

## 主题/图标问题

症状: 首次渲染主题闪烁、iconfont 缺失、图标与后台/预览不同

检查顺序:
1. `app.tsx` 中主题预加载请求
2. 本地主题缓存 key
3. iconfont URL: `https://kkl.top/uploads/fonts/iconfont.ttf`
4. 页面是否使用了approved 图标组件

**关键**: 图标全局消失 → 检查服务器 `/uploads/fonts/`（已知生产事故模式）

## 包大小 / 80051 错误

检查顺序:
1. 新功能是否放在了主包而非分包
2. 大资源是否打包而非远程加载
3. Wrapper 是否向主包导入了过多共享代码
4. 是否需要清理构建 (`pnpm miniapp:build:clean`)

## 何时升级到非小程序调查

以下情况不要继续修 wrapper:
- 后台和小程序都显示错误的订单/支付状态
- escort token 有效但接口仍失败
- 就诊人 API 返回成功但持久化数据错误
- 客服会话状态在源头就是错的
