---
description: 科科灵项目管理规则。处理客户反馈、Bug 分类、优先级判定、发版协调、交付沟通时自动应用。
alwaysApply: true
---

# 科科灵 项目管理规则

不要把所有客户反馈当作同类工作。先分类，再决定处理方式。

## 分类体系

收到问题后先归类：
- **生产事故**: 主链路中断，立即处理
- **Bug**: 功能异常，排期修复
- **数据问题**: 数据不一致，需查库
- **体验问题**: 交互/文案/样式，批量处理
- **新需求**: 产品范围扩展，不进入 Bug 通道
- **运维请求**: 操作支持，非代码问题

## 优先级规则

### P0 — 收入/认证/履约链路中断
- 支付成功但状态未更新 / 无法下单 / 支付后无法派单
- 陪诊员无法接单 / 提现不可用 / 核心角色登录失败
- 生产宕机或反复 502
- **处理**: 立即检查，停下非紧急工作，最小安全修复

### P1 — 主链路可用但结果错误/不完整
- 表单缺必要选项导致无法下单 / 关键计数不同步
- CRUD 操作失败 / 后台可见性滞后 / 消息缺少提示
- **处理**: 当前批次排期修复，验证相邻路径

### P2 — 局部/外观/可绕过
- 图标显示 / 页面排版 / 文案不一致 / 非阻塞交互摩擦
- **处理**: 和类似 UI 修复一起批量处理

### 需求池 — 纯新功能
- 移动端管理后台 / 新运营看板 / 生日券逻辑 / 流程重设计
- **处理**: 不分配 Bug 优先级，单独标记

## 判断准则

- 交易状态异常 > 可见 UI 缺陷
- 单角色失败但该角色对订单完成必不可少 → 仍可为 P0
- 涉及资金/订单状态/认证 → 优先级上调
- 仅措辞/样式问题 → 优先级下调（除非影响信任或转化）

## 混合反馈处理流程

1. 拆分为独立工作项
2. 每项分类 + 判断是否影响核心业务链
3. 分为: 立即修复 / 排期修复 / 待确认 / 未来需求
4. 同时输出: 内部执行顺序 + 客户回复（如需要）

## Bug 处理流程

1. 还原用户路径 → 2. 判断前端/后端/配置/数据/第三方 → 3. 评估业务影响 → 4. 最小安全修复 → 5. 验证直接路径 + 相邻路径

## 发版协调

1. 按业务链分组（非按文件数量）
2. 标出涉及支付/订单/派单/接单/提现/认证/数据迁移的变更
3. 发布前定义回归清单，发布后在目标环境验证

## 输出模板

### 内部优先级摘要
```
结论：本批问题共 X 项，其中 P0 X 项，P1 X 项，P2 X 项，新增需求 X 项。
优先处理：1. xxx  2. xxx  3. xxx
原因：- xxx 直接影响支付/下单/派单主链路
后续处理：- 当前批次修复：xxx  - 需确认：xxx  - 归入需求池：xxx
```

### 客户回复摘要
```
已收到，本次问题已拆分处理。
优先修复项：1. xxx  2. xxx
以下属于新增需求，与缺陷修复分开排期：1. xxx
我们会先处理影响下单、支付、派单、接单、提现的核心问题，再同步后续结果。
```

## 业务术语

- **派单**: 后台将订单分配给陪诊员
- **接单**: 陪诊员确认接收并进入履约流程
- **就诊人**: 下单时选择的服务对象资料
- **提现**: 平台余额向公司或个人账户结算
- "不能接单" → 优先查订单状态、角色权限、可接单条件
- "已支付但显示未支付" → 优先查支付回调、状态流转、数据库写入
