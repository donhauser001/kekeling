# 会员中心模块改造记录

> **模块名称**：会员中心（MembershipPage）  
> **改造日期**：2025-12-21  
> **基于模板**：[模块改造复盘模板 v1.1](./模块改造复盘模板.md)

---

## 1. 模块边界定义

### 1.1 涉及页面

| 页面 | 组件路径 | 功能描述 |
|------|----------|----------|
| 会员中心 | `membership/MembershipPage` | 显示用户会员状态、权益列表 |
| 会员套餐 | `marketing/MembershipPlansPage` | 展示可购买的会员套餐，提供购买功能 |

### 1.2 涉及 API

| 接口名称 | 方法 | 路径 | 功能 | 认证 |
|----------|------|------|------|------|
| 我的会员 | GET | `/membership/my` | 获取用户会员状态 | ✅ 需要 |
| 会员套餐列表 | GET | `/membership/plans` | 获取可购买的会员套餐 | ❌ 不需要 |
| 购买会员 | POST | `/membership/purchase` | 用户购买会员套餐 | ✅ 需要 |

### 1.3 数据流向图

```
┌─────────────┐     GET /membership/my       ┌─────────────┐
│   用户端    │ ◄────────────────────────── │   后端 API  │
│  (小程序)   │                              │  (NestJS)   │
│             │ ────────────────────────►    │             │
└─────────────┘    POST /membership/purchase └─────────────┘
       │                                            │
       │ 前端数据展示                               │ 数据库查询
       ▼                                            ▼
┌─────────────┐                              ┌─────────────┐
│  UI 组件    │                              │  PostgreSQL │
│  状态管理   │                              │  Prisma ORM │
└─────────────┘                              └─────────────┘
```

---

## 2. 改造内容

### 2.1 修复 API 路径不匹配

**问题描述**
- 前端调用 `/marketing/membership/my`
- 后端实际路由是 `/membership/my`
- 导致 404 错误

**⚠️ 失败信号**
```
GET https://kkl.top/api/marketing/membership/my 404 (Not Found)
```

**解决方案**
```typescript
// ❌ 错误
return await userRequest('/marketing/membership/my')

// ✅ 正确
return await userRequest('/membership/my')
```

### 2.2 拆分大文件（规则 12）

**原始状态**
- `MembershipPage.tsx` 共 722 行
- 超过 500 行规范阈值

**拆分后结构**
```
membership/
├── index.ts                    # 模块导出（19行）
├── types.ts                    # 类型定义（65行）
├── constants.ts                # 常量和工具函数（60行）
├── MembershipCard.tsx          # 会员卡片组件（210行）
├── BenefitsGrid.tsx            # 权益网格组件（75行）
├── MembershipPageSkeleton.tsx  # 骨架屏组件（195行）
├── NoMembership.tsx            # 未开通状态组件（95行）
├── ErrorState.tsx              # 错误状态组件（55行）
└── MembershipPage.tsx          # 主组件（210行）
```

**拆分原则**
- 每个组件单一职责
- 主组件控制在 300 行以内
- 类型和常量独立文件

### 2.3 添加购买会员功能

**新增 API**
```typescript
/**
 * 购买会员套餐
 * 接口: POST /membership/purchase
 * 通道: userRequest
 */
export const purchaseMembership = async (planId: string): Promise<{
  success: boolean
  orderId?: string
  message?: string
}> => {
  try {
    const response = await userRequest<{
      orderId: string
      amount: number
      status: string
    }>('/membership/purchase', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify({ planId }),
    })
    return { success: true, orderId: response.orderId }
  } catch (error) {
    if (error instanceof ApiError) {
      return { success: false, message: error.message }
    }
    return { success: false, message: '网络错误，请重试' }
  }
}
```

**MembershipPlansPage 购买状态管理**
```typescript
// 购买相关状态
const [isPurchasing, setIsPurchasing] = useState(false)
const [purchaseResult, setPurchaseResult] = useState<{
  success: boolean
  message: string
} | null>(null)

// 处理购买会员
const handlePurchase = async () => {
  if (!selectedPlanId || isPurchasing) return

  setIsPurchasing(true)
  setPurchaseResult(null)

  try {
    const result = await previewApi.purchaseMembership(selectedPlanId)
    if (result.success) {
      setPurchaseResult({
        success: true,
        message: '订单创建成功，请完成支付',
      })
    } else {
      setPurchaseResult({
        success: false,
        message: result.message || '购买失败，请重试',
      })
    }
  } catch (error: any) {
    setPurchaseResult({
      success: false,
      message: error.message || '网络错误，请重试',
    })
  } finally {
    setIsPurchasing(false)
  }
}
```

### 2.4 数据结构对齐

**MembershipInfo 类型更新**
```typescript
export interface MembershipInfo {
  id: string
  level: string
  levelName: string
  expireAt: string
  points: number
  daysLeft?: number        // 新增：剩余天数
  discount?: number        // 新增：折扣率
  overtimeFeeWaiver?: number  // 新增：超时费减免
}
```

**MembershipPlan 类型更新**
```typescript
export interface MembershipPlan {
  id: string
  name: string
  description?: string
  price: number           // 价格（元）
  originalPrice?: number  // 原价（元）
  duration: number        // 字段名修正：durationDays → duration
  recommended?: boolean   // 字段名修正：isRecommended → recommended
  features?: string[]     // 新增：套餐特点
  level?: {              // 新增：等级信息
    id: string
    name: string
    code?: string
    icon?: string
    color?: string
    discount?: number
  }
}
```

---

## 3. 改造检查清单

### 3.1 后端检查

- [x] API 路由已与前端调用一致（`/membership/*`）
- [x] 需要用户身份的接口已添加 `@UseGuards(JwtAuthGuard)`
- [x] 已添加 `@ApiBearerAuth()` 供 Swagger 文档使用

### 3.2 前端 API 层检查

- [x] API 路径已修正为 `/membership/my` 和 `/membership/plans`
- [x] POST 请求已设置 `Content-Type: application/json`
- [x] 已在 API 层进行数据格式转换
- [x] 错误处理已解析后端返回的实际错误消息
- [x] 接口类型定义已完整

### 3.3 前端组件层检查

- [x] 正确使用后端返回的状态字段（daysLeft, discount 等）
- [x] UI 状态有集中的判断逻辑
- [x] 加载状态、错误状态、空状态都有处理
- [x] 操作按钮有 loading 和 disabled 状态
- [x] 用户反馈（弹窗提示）清晰

### 3.4 跨平台兼容检查

- [x] 使用的组件都在 `primitives` 中导出
- [x] 使用 `wxScale` 处理尺寸
- [x] 导航栏符合规范（主色背景 + 标题居中 + Icon 返回按钮）

### 3.5 文件结构检查

- [x] 大文件已拆分（722行 → 8个文件）
- [x] 每个文件控制在 300 行以内
- [x] 类型定义独立文件
- [x] 常量和工具函数独立文件

---

## 4. 文件变更清单

### 新增文件

| 文件路径 | 行数 | 说明 |
|----------|------|------|
| `membership/index.ts` | 19 | 模块导出 |
| `membership/types.ts` | 65 | 类型定义 |
| `membership/constants.ts` | 60 | 常量和工具函数 |
| `membership/MembershipCard.tsx` | 210 | 会员卡片组件 |
| `membership/BenefitsGrid.tsx` | 75 | 权益网格组件 |
| `membership/MembershipPageSkeleton.tsx` | 195 | 骨架屏组件 |
| `membership/NoMembership.tsx` | 95 | 未开通状态组件 |
| `membership/ErrorState.tsx` | 55 | 错误状态组件 |
| `membership/MembershipPage.tsx` | 210 | 主组件（精简后） |

### 修改文件

| 文件路径 | 说明 |
|----------|------|
| `api/types.ts` | 更新 MembershipInfo、MembershipPlan 类型定义 |
| `api/user-api.ts` | 修复 API 路径，新增 purchaseMembership |
| `api/index.ts` | 导出 purchaseMembership |
| `marketing/MembershipPlansPage.tsx` | 添加购买功能、结果弹窗 |
| `marketing/index.ts` | 移除 MembershipPage 导出（已迁移） |
| `pages/index.ts` | 更新 MembershipPage 导入路径 |
| `mocks/marketing.ts` | 更新 mock 数据字段名 |

### 删除文件

| 文件路径 | 说明 |
|----------|------|
| `marketing/MembershipPage.tsx` | 已迁移到 membership/ 目录 |

---

## 5. 更新日志

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0 | 2025-12-21 | 初始改造：修复 API 路径、拆分大文件、添加购买功能、对齐数据结构 |

---

> **一句话总结**：会员中心模块已完成小程序规范改造，修复 API 路径、拆分为 8 个模块化组件、新增购买会员功能。

