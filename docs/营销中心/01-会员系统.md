# 会员系统规划文档

> 返回 [营销中心总览](./README.md)  
> 状态：✅ 已规划

---

## 一、核心概念

### 1.1 会员模型说明

本系统采用 **等级+套餐** 模式：

```
会员等级（权益配置）          会员套餐（购买方式）
┌─────────────────┐          ┌─────────────────┐
│ VIP会员          │          │ 月卡 ¥29        │
│ · 9折服务折扣    │◄─────────│ · VIP会员等级   │
│ · 50%超时减免    │          │ · 30天有效期    │
│ · 优先接单      │          │ · 续费送3天     │
│ · 每月2张券     │          ├─────────────────┤
└─────────────────┘          │ 季卡 ¥69        │
                             │ · VIP会员等级   │
                             │ · 90天有效期    │
                             │ · 续费送10天    │
                             ├─────────────────┤
                             │ 年卡 ¥199       │
                             │ · VIP会员等级   │
                             │ · 365天有效期   │
                             │ · 续费送30天    │
                             └─────────────────┘
```

**设计说明**：
- 当前版本采用单一等级（VIP会员），通过套餐区分时长和价格
- 未来可扩展为多等级（银卡/金卡/钻石），套餐关联不同等级

### 1.2 会员权益

| 权益类型 | 普通用户 | VIP会员 |
|---------|---------|---------|
| 服务折扣 | 无 | 9折 |
| 超时费减免 | 无 | 50% |
| 优先接单 | ✗ | ✓ |
| 每月优惠券 | 无 | 2张 |
| 专属客服 | ✗ | ✓ |
| 生日礼券 | 无 | ¥20 |

---

## 二、数据模型

### 2.1 会员等级

```prisma
model MembershipLevel {
  id                String   @id @default(uuid())
  name              String                          // VIP会员
  code              String   @unique                // vip
  icon              String?                         // 等级图标
  color             String?                         // #FFD700
  discount          Int      @default(100)          // 90 = 9折
  overtimeFeeWaiver Int      @default(0)            // 50 = 50%减免
  benefits          Json                            // 权益详情
  sort              Int      @default(0)
  status            String   @default("active")
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
  
  plans             MembershipPlan[]
  userMemberships   UserMembership[]
  upgradeRules      ConsumeUpgradeRule[]
  
  @@map("membership_levels")
}
```

**benefits JSON 结构**：
```typescript
interface MembershipBenefits {
  priorityBooking: boolean;       // 优先接单
  dedicatedSupport: boolean;      // 专属客服
  birthdayCouponAmount: number;   // 生日礼券金额
  monthlyCouponCount: number;     // 每月优惠券数量
  monthlyCouponTemplateId?: string; // 每月发放的优惠券模板
}
```

### 2.2 会员套餐

```prisma
model MembershipPlan {
  id              String   @id @default(uuid())
  levelId         String   @map("level_id")
  name            String                              // 月卡/季卡/年卡
  code            String                              // monthly/quarterly/yearly
  price           Decimal  @db.Decimal(10, 2)         // 29/69/199
  originalPrice   Decimal? @db.Decimal(10, 2)         // 原价（展示用）
  duration        Int                                 // 30/90/365 天
  renewalBonus    Int      @default(0)                // 续费赠送天数
  description     String?
  features        String[]                            // 卖点列表
  sort            Int      @default(0)
  recommended     Boolean  @default(false)            // 推荐标记
  status          String   @default("active")
  createdAt       DateTime @default(now())
  updatedAt       DateTime @updatedAt
  
  level           MembershipLevel @relation(fields: [levelId], references: [id])
  orders          MembershipOrder[]
  
  @@unique([levelId, code])  // 同一等级下 code 唯一
  @@map("membership_plans")
}
```

### 2.3 用户会员记录

```prisma
model UserMembership {
  id           String   @id @default(uuid())
  userId       String   @map("user_id")
  levelId      String   @map("level_id")
  planId       String?  @map("plan_id")
  
  // 来源
  source       String   @default("purchase")
  // purchase: 购买套餐
  // consume: 消费升级
  // gift: 赠送
  // compensate: 补偿
  
  // 快照（下单时记录，不受后续修改影响）
  levelName    String   @map("level_name")
  discount     Int
  overtimeFeeWaiver Int @map("overtime_fee_waiver")
  
  // 有效期
  startAt      DateTime @map("start_at")
  expireAt     DateTime @map("expire_at")
  
  // 状态
  status       String   @default("active")
  // active: 生效中
  // expired: 已过期
  // cancelled: 已取消（退款）
  
  createdAt    DateTime @default(now())
  
  user         User     @relation(fields: [userId], references: [id])
  level        MembershipLevel @relation(fields: [levelId], references: [id])
  
  @@index([userId, status])
  @@index([expireAt])
  @@map("user_memberships")
}
```

### 2.4 会员订单

```prisma
model MembershipOrder {
  id            String    @id @default(uuid())
  orderNo       String    @unique @map("order_no")
  userId        String    @map("user_id")
  planId        String    @map("plan_id")
  
  // 订单类型
  type          String    @default("purchase")
  // purchase: 新购
  // renew: 续费
  // upgrade: 升级（预留）
  
  // 快照
  planName      String    @map("plan_name")
  planPrice     Decimal   @db.Decimal(10, 2) @map("plan_price")
  duration      Int                                    // 购买天数
  bonusDays     Int       @default(0) @map("bonus_days") // 赠送天数
  
  // 支付
  amount        Decimal   @db.Decimal(10, 2)           // 实付金额
  paymentMethod String?   @map("payment_method")       // wechat/alipay
  paidAt        DateTime? @map("paid_at")
  
  // 状态
  status        String    @default("pending")
  // pending: 待支付
  // paid: 已支付
  // cancelled: 已取消
  // refunded: 已退款
  
  // 退款
  refundedAt    DateTime? @map("refunded_at")
  refundAmount  Decimal?  @db.Decimal(10, 2) @map("refund_amount")
  refundReason  String?   @map("refund_reason")
  
  createdAt     DateTime  @default(now())
  
  user          User      @relation(fields: [userId], references: [id])
  plan          MembershipPlan @relation(fields: [planId], references: [id])
  
  @@index([userId])
  @@map("membership_orders")
}
```

### 2.5 消费升级规则

```prisma
model ConsumeUpgradeRule {
  id           String   @id @default(uuid())
  levelId      String   @map("level_id")
  threshold    Decimal  @db.Decimal(10, 2)   // 累计消费门槛
  grantDays    Int      @map("grant_days")   // 赠送会员天数
  
  // 统计周期
  period       String   @default("forever")
  // forever: 累计总消费
  // yearly: 年度消费
  // monthly: 月度消费
  
  description  String?
  sort         Int      @default(0)
  status       String   @default("active")
  createdAt    DateTime @default(now())
  
  level        MembershipLevel @relation(fields: [levelId], references: [id])
  
  @@map("consume_upgrade_rules")
}
```

---

## 三、服务级会员配置

### 3.1 Service 模型扩展

```prisma
model Service {
  // ... 现有字段 ...
  
  // 会员政策
  membershipPolicy          String  @default("normal") @map("membership_policy")
  // normal: 响应会员折扣
  // exclusive: 会员专属（普通用户不可购买）
  // fixed: 固定价格（不响应会员折扣）
  
  // 服务专属设置（覆盖等级默认值）
  membershipDiscount        Int?    @map("membership_discount")        // 专属折扣
  membershipOvertimeWaiver  Int?    @map("membership_overtime_waiver") // 专属超时减免
}
```

### 3.2 Workflow 模型扩展

```prisma
model Workflow {
  // ... 现有字段 ...
  
  // 会员超时费减免（服务可覆盖）
  membershipOvertimeWaiver  Int  @default(0) @map("membership_overtime_waiver")
}
```

---

## 四、业务逻辑

### 4.1 续费逻辑

```typescript
async function renewMembership(userId: string, planId: string) {
  const current = await getCurrentActiveMembership(userId);
  const plan = await getPlan(planId);
  
  let startAt: Date;
  let expireAt: Date;
  
  if (current && current.expireAt > new Date()) {
    // 有效期内续费：在原有效期基础上叠加
    startAt = current.startAt;
    expireAt = addDays(current.expireAt, plan.duration + plan.renewalBonus);
  } else {
    // 新购或已过期：从当前时间开始
    startAt = new Date();
    expireAt = addDays(startAt, plan.duration + plan.renewalBonus);
  }
  
  return createMembershipOrder({
    userId,
    planId,
    type: current ? 'renew' : 'purchase',
    startAt,
    expireAt,
    bonusDays: plan.renewalBonus,
  });
}
```

### 4.2 消费升级逻辑

```typescript
async function checkConsumeUpgrade(userId: string, orderAmount: number) {
  // 获取用户累计消费
  const totalConsume = await getUserTotalConsume(userId);
  const newTotal = totalConsume + orderAmount;
  
  // 检查是否触发升级规则
  const rules = await getActiveUpgradeRules();
  for (const rule of rules) {
    if (totalConsume < rule.threshold && newTotal >= rule.threshold) {
      // 触发升级
      await grantMembership({
        userId,
        levelId: rule.levelId,
        source: 'consume',
        days: rule.grantDays,
      });
    }
  }
}
```

### 4.3 会员并存规则

当用户同时拥有「购买会员」和「消费升级会员」时：

```typescript
function getEffectiveMembership(memberships: UserMembership[]) {
  // 1. 过滤有效的会员记录
  const active = memberships.filter(m => 
    m.status === 'active' && m.expireAt > new Date()
  );
  
  if (active.length === 0) return null;
  
  // 2. 取折扣最优的（数值最小 = 折扣最大）
  return active.reduce((best, current) => 
    current.discount < best.discount ? current : best
  );
}
```

---

## 五、退款处理

### 5.1 会员退款规则

```typescript
async function refundMembership(orderId: string) {
  const order = await getMembershipOrder(orderId);
  const membership = await getUserMembershipByOrder(orderId);
  
  // 计算已使用天数
  const usedDays = differenceInDays(new Date(), membership.startAt);
  const totalDays = order.duration + order.bonusDays;
  
  // 计算退款金额（按天计算，赠送天数不计入）
  const dailyPrice = order.amount / order.duration;
  const refundDays = Math.max(0, order.duration - usedDays);
  const refundAmount = dailyPrice * refundDays;
  
  // 执行退款
  await transaction([
    // 更新订单状态
    updateOrder(orderId, { 
      status: 'refunded', 
      refundAmount,
      refundedAt: new Date() 
    }),
    // 失效会员
    updateMembership(membership.id, { status: 'cancelled' }),
  ]);
  
  return refundAmount;
}
```

---

## 六、权益实现说明

### 6.1 优先接单

```typescript
// 订单匹配时，会员订单优先级 +10
function calculateOrderPriority(order: Order) {
  let priority = 0;
  
  if (order.membership?.benefits?.priorityBooking) {
    priority += 10;
  }
  
  // 其他优先级因素...
  return priority;
}
```

### 6.2 专属客服

- 会员用户进入客服时，自动分配到专属客服队列
- 后台客服系统显示会员标识

### 6.3 每月优惠券发放

```typescript
// 每月1号凌晨执行
async function grantMonthlyCoupons() {
  const members = await getActiveMembers();
  
  for (const member of members) {
    const benefits = member.level.benefits;
    if (benefits.monthlyCouponCount > 0) {
      await grantCoupons({
        userId: member.userId,
        templateId: benefits.monthlyCouponTemplateId,
        quantity: benefits.monthlyCouponCount,
        source: 'member_monthly',
      });
    }
  }
}
```

### 6.4 会员到期提醒

```typescript
// 每天执行，检查7天内到期的会员
async function sendExpirationReminders() {
  const expiringSoon = await getMembersExpiringWithinDays(7);
  
  for (const member of expiringSoon) {
    const daysLeft = differenceInDays(member.expireAt, new Date());
    
    await sendNotification({
      userId: member.userId,
      template: 'membership_expiring',
      data: { daysLeft, expireAt: member.expireAt },
    });
  }
}
```

---

## 七、API 设计

### 7.1 用户端

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/membership/levels | 获取会员等级列表 |
| GET | /api/membership/plans | 获取会员套餐列表 |
| GET | /api/membership/my | 获取当前用户会员状态 |
| POST | /api/membership/purchase | 购买/续费会员 |
| GET | /api/membership/orders | 我的会员订单 |
| POST | /api/membership/orders/:id/refund | 申请退款 |

### 7.2 后台管理

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/admin/membership/levels | 等级列表 |
| POST | /api/admin/membership/levels | 创建等级 |
| PUT | /api/admin/membership/levels/:id | 更新等级 |
| GET | /api/admin/membership/plans | 套餐列表 |
| POST | /api/admin/membership/plans | 创建套餐 |
| GET | /api/admin/membership/users | 会员用户列表 |
| POST | /api/admin/membership/grant | 手动发放会员 |
| GET | /api/admin/membership/upgrade-rules | 消费升级规则 |

---

## 八、用户界面

### 8.1 会员中心页

```
┌─────────────────────────────────────────┐
│  ←  会员中心                             │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │         👑 VIP 会员              │   │
│  │                                  │   │
│  │  选择套餐                        │   │
│  │                                  │   │
│  │  ┌────────┐ ┌────────┐ ┌────────┐│   │
│  │  │ 月卡   │ │ 季卡   │ │ 年卡 ⭐││   │
│  │  │ ¥29    │ │ ¥69    │ │ ¥199   ││   │
│  │  │ 30天   │ │ 90天   │ │ 365天  ││   │
│  │  │        │ │ 省¥18  │ │ 省¥149 ││   │
│  │  └────────┘ └────────┘ └────────┘│   │
│  │                        推荐 ↑    │   │
│  └─────────────────────────────────┘   │
│                                         │
│  会员权益                               │
│  ┌─────────────────────────────────┐   │
│  │ 💰 服务9折      所有服务享9折    │   │
│  │ ⏰ 超时减免      超时费减免50%   │   │
│  │ ⚡ 优先接单      订单优先匹配    │   │
│  │ 🎫 每月2张券    每月自动发放     │   │
│  │ 📞 专属客服      7×24小时服务   │   │
│  │ 🎂 生日礼券      ¥20生日券      │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │      [立即开通 - ¥199/年]        │   │
│  └─────────────────────────────────┘   │
│                                         │
└─────────────────────────────────────────┘
```

### 8.2 个人中心会员卡片

```
已开通会员：
┌─────────────────────────────────────────┐
│ 👑 VIP会员                   [查看权益] │
│                                         │
│ 有效期至 2025-12-31          还剩386天  │
│                                         │
│ ┌─────────┐ ┌─────────┐ ┌─────────┐   │
│ │ 9折     │ │ 50%    │ │ 2张     │   │
│ │ 服务折扣│ │ 超时减免│ │ 本月券  │   │
│ └─────────┘ └─────────┘ └─────────┘   │
│                                         │
│ [续费]                                  │
└─────────────────────────────────────────┘

未开通会员：
┌─────────────────────────────────────────┐
│ 🎫 开通VIP会员，尊享专属特权            │
│                                         │
│ ✓ 全场9折  ✓ 超时费减免50%  ✓ 每月2张券│
│                                         │
│ [¥199/年 开通 →]                        │
└─────────────────────────────────────────┘
```
