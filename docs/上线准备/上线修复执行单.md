**上线修复执行单（完整版）**

**0. 发布策略（先决）**
1. 立即冻结发布分支，禁止合并新功能，仅允许修复。
2. 设立上线门禁：`frontend build/lint`、`server build/lint/test`、安全回归全部通过才允许发布。
3. 建议创建修复分支：`codex/release-hardening-2026-02-28`。

**1. P0（必须先完成，阻断上线）**

1. 下线测试模块与测试接口
- 目标文件：[server/src/app.module.ts](/Users/aiden/Documents/app/kekeling/server/src/app.module.ts)  
- 目标文件：[server/src/modules/test/test.module.ts](/Users/aiden/Documents/app/kekeling/server/src/modules/test/test.module.ts)  
- 目标文件：[server/src/modules/test/test.controller.ts](/Users/aiden/Documents/app/kekeling/server/src/modules/test/test.controller.ts)
- 动作：
1. 移除 `TestModule` 生产加载（按 `NODE_ENV !== 'production'` 条件加载或直接删除）。
2. 禁止 `test/*` 路由进入生产包。
- 验收：
1. 生产环境无 `/api/test/*` 路由。
2. OpenAPI 无测试接口。

2. 修复管理端未鉴权接口
- 目录：[server/src/modules/admin/controllers](/Users/aiden/Documents/app/kekeling/server/src/modules/admin/controllers)
- 重点文件示例：[server/src/modules/admin/controllers/admin-users.controller.ts](/Users/aiden/Documents/app/kekeling/server/src/modules/admin/controllers/admin-users.controller.ts)  
- 重点文件示例：[server/src/modules/admin/controllers/admin-dashboard.controller.ts](/Users/aiden/Documents/app/kekeling/server/src/modules/admin/controllers/admin-dashboard.controller.ts)
- 动作：
1. 所有 `admin/*` 控制器统一加 `@UseGuards(AdminGuard)`。
2. 清理仅 `JwtAuthGuard` 的管理端接口，统一收敛到管理员守卫。
- 验收：
1. 未登录返回 `401`。
2. 普通用户 token 返回 `403`。
3. 管理员 token 可正常访问。

3. 修复 `AdminGuard` 权限绕过
- 目标文件：[server/src/modules/auth/guards/admin.guard.ts](/Users/aiden/Documents/app/kekeling/server/src/modules/auth/guards/admin.guard.ts)
- 动作：
1. 在 `super.canActivate` 后强制校验 `request.user.type === 'admin'`。
2. 校验管理员状态、角色白名单（至少 `admin/superadmin`）。
3. 拒绝非管理员与禁用管理员。
- 验收：
1. 普通用户 token 无法访问任意管理接口。
2. 被禁用管理员返回 `403`。

4. 封闭系统配置写接口（高危）
- 目标文件：[server/src/modules/config/config.controller.ts](/Users/aiden/Documents/app/kekeling/server/src/modules/config/config.controller.ts)
- 动作：
1. 整个 `config` 控制器加 `AdminGuard`。
2. 对读接口按需区分公开与管理读取。
- 验收：
1. 未授权不能读写支付/短信/营销配置。
2. 管理员可控访问。

5. 支付回调验签与幂等修复
- 目标文件：[server/src/modules/payment/payment.service.ts](/Users/aiden/Documents/app/kekeling/server/src/modules/payment/payment.service.ts)
- 动作：
1. 对 `notify` 回调实现签名验证（微信字段验签）。
2. 校验 `mch_id`、`appid`、金额与订单金额一致。
3. 增加幂等保护（已支付订单重复通知不重复写入）。
4. 异常时严格返回失败响应。
- 验收：
1. 伪造回调无法改订单状态。
2. 重放通知不会重复入账或重复通知。

6. 立即处理证书/私钥泄露
- 涉及文件：[server/ssl/key.pem](/Users/aiden/Documents/app/kekeling/server/ssl/key.pem)  
- 涉及文件：[ssl_cert/kkl.top.key](/Users/aiden/Documents/app/kekeling/ssl_cert/kkl.top.key)
- 动作：
1. 立刻轮换线上证书与私钥。
2. 从仓库删除密钥文件并加入 `.gitignore`。
3. 清理历史提交中的密钥（必要时重写历史）。
- 验收：
1. 仓库当前与历史不再包含可用私钥。
2. 新证书已生效，旧证书吊销/失效。

**2. P1（P0完成后立即执行）**

1. CMS 设置接口分级鉴权
- 目标文件：[server/src/modules/cms/settings/settings.controller.ts](/Users/aiden/Documents/app/kekeling/server/src/modules/cms/settings/settings.controller.ts)
- 动作：
1. `public` 保留公开只读。
2. `create/update/delete` 全部加管理员守卫。
- 验收：
1. 公开接口仅返回允许公开的键。
2. 写操作仅管理员可用。

2. 前台富文本 XSS 治理（统一 SafeHTML）
- 目标文件：[website/src/pages/HomePage.tsx](/Users/aiden/Documents/app/kekeling/website/src/pages/HomePage.tsx)  
- 目标文件：[website/src/pages/ArticleDetailPage.tsx](/Users/aiden/Documents/app/kekeling/website/src/pages/ArticleDetailPage.tsx)  
- 目标文件：[website/src/pages/PageDetail.tsx](/Users/aiden/Documents/app/kekeling/website/src/pages/PageDetail.tsx)  
- 目标文件：[website/src/pages/ServiceDetailPage.tsx](/Users/aiden/Documents/app/kekeling/website/src/pages/ServiceDetailPage.tsx)  
- 目标文件：[website/src/components/SidebarRenderer.tsx](/Users/aiden/Documents/app/kekeling/website/src/components/SidebarRenderer.tsx)
- 参考组件：[src/components/ui/safe-html.tsx](/Users/aiden/Documents/app/kekeling/src/components/ui/safe-html.tsx)
- 动作：
1. 替换直接 `dangerouslySetInnerHTML` 为统一净化组件。
2. 增加白名单策略（标签、属性、协议）。
- 验收：
1. ESLint `react/no-danger` 在 `website` 下无违规。
2. XSS payload（`<script>`, `onerror`, `javascript:`）被拦截。

3. CSRF 与 CORS 策略收敛
- 目标文件：[server/src/main.ts](/Users/aiden/Documents/app/kekeling/server/src/main.ts)
- 动作：
1. 将 `origin: true` 改为显式 allowlist。
2. 对 cookie 鉴权接口启用 CSRF 防护（token 或双提交 cookie）。
- 验收：
1. 非白名单源无法携带凭据调用。
2. 跨站伪造请求被拒绝。

4. 模拟支付接口再加一层防护
- 目标文件：[server/src/modules/payment/payment.controller.ts](/Users/aiden/Documents/app/kekeling/server/src/modules/payment/payment.controller.ts)
- 动作：
1. `mock-pay` 增加 `AdminGuard`。
2. 保留 `NODE_ENV + ENABLE_MOCK_PAYMENT` 双开关。
- 验收：
1. 生产环境、非管理员、未开启开关均不可调用。

**3. P2（稳定性与发布质量）**

1. 修复 lint/build 基线
- 前端配置：[eslint.config.js](/Users/aiden/Documents/app/kekeling/eslint.config.js)
- 动作：
1. 先修阻断型错误（TS、no-console、类型不匹配）。
2. 分批处理 warning，最终降到可控阈值（建议 0 error）。
- 验收：
1. `pnpm lint` 通过。
2. `pnpm build` 通过。

2. 修复 server lint 版本冲突
- 目标文件：[server/package.json](/Users/aiden/Documents/app/kekeling/server/package.json)
- 动作：
1. 统一 ESLint 与 `@typescript-eslint` 主版本（避免 8/9 混用）。
2. 固定 lockfile，避免根目录依赖污染 server lint。
- 验收：
1. `cd server && pnpm lint` 可稳定执行并通过。

3. 补齐最小回归测试
- 目标目录：[server/test](/Users/aiden/Documents/app/kekeling/server/test)
- 动作：
1. 为鉴权、支付回调、配置写接口、管理权限添加 e2e。
2. 最低覆盖关键链路，不追求大面积覆盖率。
- 验收：
1. `server` 至少有可执行 e2e 套件。
2. 核心安全用例均通过。

**4. 执行顺序（建议）**
1. Day 1: P0-1 ~ P0-4（测试模块、管理鉴权、AdminGuard、Config鉴权）。
2. Day 2: P0-5 ~ P0-6（支付验签、密钥轮换）。
3. Day 3: P1（CMS鉴权、XSS、CORS/CSRF、mock-pay加强）。
4. Day 4: P2（lint/build、server lint 依赖统一、最小e2e）。
5. Day 5: 预发回归 + 安全复测 + 灰度发布。

**5. 最终放行门禁（必须全部满足）**
1. 无 `P0/P1` 未关闭项。
2. 前后端构建、lint、关键测试全绿。
3. 无测试/模拟/调试接口暴露。
4. 管理接口鉴权和角色校验通过渗透式抽测。
5. 支付回调伪造与重放测试通过。
6. 密钥轮换完成并审计留痕。

如果你愿意，我可以下一步直接按这个执行单进入“P0 修复落地”，从 `admin 鉴权 + TestModule 下线` 开始逐文件提交。
# 上线修复执行单（修订版）

## 1. 目标与范围

- 目标：在不引入新功能的前提下，完成上线前安全加固与质量门禁，消除 P0/P1 风险。
- 范围：`server`（鉴权、支付、配置、测试接口、密钥治理）、`website`（XSS 渲染链路）、工程质量门禁（lint/build/test）。

## 2. 优先级（修订后）

### P0（阻断上线，必须先修）

1. 密钥治理（证书/私钥泄露）：立即轮换并从仓库移除敏感密钥资产。
2. 管理员权限守卫修复：`AdminGuard` 禁止“已登录即放行”。
3. 管理接口鉴权统一：`admin/*` 全量收敛到 `AdminGuard`。
4. 配置中心鉴权（资金链路）：封闭 `config` 的支付/短信等写接口，防止被任意篡改。
5. 支付回调验签与幂等：补齐验签、金额校验、幂等保护。
6. 下线测试模块：生产不加载 `TestModule` 与 `test/*` 接口。

### P1（P0 完成后立即执行）

1. CMS 设置接口分级鉴权：`public` 只读保留，其余接口收敛至管理员。
2. 前台富文本 XSS 治理：统一使用 `SafeHTML`，禁用裸 `dangerouslySetInnerHTML`。
3. CORS 收敛：`origin: true` 改白名单，禁止任意源携带凭据。
4. `mock-pay` 二次加固：管理员守卫 + 环境开关双保险。
5. 数据库密码治理：替换弱口令（例如带 `dev` 标识密码），改为强随机密码并完成部署同步。

### P2（质量与可运维）

1. 修复 lint/build 阻断项并建立可持续基线。
2. 修复 server ESLint 版本冲突（避免 8/9 混用）。
3. 补齐关键链路 e2e（鉴权、支付、配置高危写口）。

## 3. 执行顺序（修订后）

| 时间 | 内容 | 理由 |
|---|---|---|
| Day 1 上午 | 密钥轮换 + AdminGuard 修复 | 先堵住最致命漏洞 |
| Day 1 下午 | 管理接口鉴权统一 + Config/CMS 鉴权 | 关闭无鉴权写入口 |
| Day 2 | TestModule 下线 + 支付验签幂等 | 清理测试接口 + 加固支付 |
| Day 3 | XSS + CORS + mock-pay | P1 安全加固 |
| Day 4 | lint/build + e2e | 质量保障 |
| Day 5 | 回归 + 灰度发布 | 上线前最终确认 |

## 4. Day 1 执行记录（持续更新）

### Day 1 上午（进行中）

- [x] S1.1 密钥治理（仓库层）
  - [x] 从 Git 索引移除已跟踪私钥/证书文件（保留本地文件，不再纳入版本控制）。
  - [x] `.gitignore` 补充敏感路径忽略规则。
  - [ ] 记录线上密钥轮换与证书吊销操作窗口（运维执行项，待执行）。
- [x] S1.2 `AdminGuard` 修复
  - [x] 先执行 `super.canActivate(context)` 验证 JWT。
  - [x] 强制校验 `user.type === 'admin'` 与管理员状态/角色。
  - [x] 非管理员统一拒绝（403）。

### Day 1 下午（已完成）

- [x] S1.3 `admin/*` 控制器鉴权统一为 `@UseGuards(AdminGuard)`。
- [x] S1.4 `config` 控制器加管理员鉴权（含支付/短信配置写接口）。
- [x] S1.5 `cms/settings` 分级鉴权（`public` 保留公开，其余管理员）。

## 6. Day 1 变更记录（代码）

### 阶段 A（上午）密钥治理 + AdminGuard

- 变更文件：
  - `.gitignore`
  - `server/src/modules/auth/guards/admin.guard.ts`
- 索引移除（不再纳入版本控制）：
  - `server/ssl/cert.pem`
  - `server/ssl/key.pem`
  - `ssl_cert/kkl.top.pem`
  - `ssl_cert/kkl.top.key`
- 结果：
  - 仓库层敏感证书/密钥已停止跟踪。
  - 管理员守卫不再“登录即放行”，已增加管理员身份与角色校验。

### 阶段 B（下午）管理接口与配置面收口

- 变更文件：
  - `server/src/modules/admin/controllers/*.ts`（全量 `AdminGuard` 收口）
  - `server/src/modules/config/config.controller.ts`（控制器级 `AdminGuard`）
  - `server/src/modules/cms/settings/settings.controller.ts`（`public` 公开，其余管理员）
- 结果：
  - `admin/*` 控制器统一管理员守卫。
  - `config` 高危写接口（支付/短信配置）已进入管理员鉴权保护。
  - `cms/settings` 仅 `GET /public` 保持公开读取。

## 5. 验收标准（Day 1）

- 管理接口：未登录 401、普通用户 403、管理员可访问。
- 配置高危接口：非管理员不可读写（尤其支付/短信配置）。
- 仓库合规：私钥/证书不再被跟踪；完成轮换计划登记。
