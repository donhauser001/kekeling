# 完整示例

参考以下文件作为完整实现示例：

## Dialog 弹窗示例

### 员工新建/编辑弹窗（标准表单 Dialog）

文件路径：`src/features/hr/components/employees-action-dialog.tsx`

特点：
- 水平布局 `grid grid-cols-6`
- 响应式高度 `max-h-[60vh] min-h-[300px]`
- 脏表单拦截
- 创建/编辑模式切换

### 员工邀请弹窗（简单表单 Dialog）

文件路径：`src/features/hr/components/employees-invite-dialog.tsx`

特点：
- 垂直布局 `space-y-4`
- 较窄宽度 `sm:max-w-md`
- 简单字段

### 员工删除弹窗（确认弹窗）

文件路径：`src/features/hr/components/employees-delete-dialog.tsx`

特点：
- 使用 `ConfirmDialog` 组件
- `destructive` 样式
- 危险操作提示

---

## Sheet 抽屉示例

### 主题配置抽屉（配置类 Sheet）

文件路径：`src/components/config-drawer.tsx`

特点：
- 分组标题
- 多个配置区域
- 右侧弹出

---

## 多板块弹窗示例

### 分销配置弹窗（Tabs + Dialog）

文件路径：`src/features/distribution/components/config-dialog.tsx`

特点：
- Tabs 分组
- Card 布局
- 多字段配置

---

## 营销中心弹窗示例

以下为按规范改造后的营销中心弹窗：

| 模块 | 文件路径 |
|------|---------|
| 活动管理 | `src/features/marketing/campaigns/components/campaigns-action-dialog.tsx` |
| 优惠券模板 | `src/features/marketing/coupons/components/coupons-action-dialog.tsx` |
| 会员等级 | `src/features/marketing/membership/components/membership-action-dialog.tsx` |
| 积分规则 | `src/features/marketing/points/components/points-action-dialog.tsx` |
| 邀请规则 | `src/features/marketing/referrals/components/referral-rules-action-dialog.tsx` |

特点：
- 多列布局 `grid grid-cols-2 items-start gap-4`
- 脏表单拦截
- onSuccess 回调
- min-height 设置

---

## 状态管理示例

### 弹窗聚合

文件路径：`src/features/hr/components/employees-dialogs.tsx`

### 状态管理 Provider

文件路径：`src/features/hr/components/employees-provider.tsx`
