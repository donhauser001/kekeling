# 工作台系统规划文档

> 返回 [陪诊员中心总览](./README.md)  
> 状态：✅ 已规划（基础功能已实现）

---

## 一、核心概念

### 1.1 工作台定位

工作台是陪诊员的核心操作界面，提供：
- **统计看板**：实时数据概览
- **快捷入口**：高频操作直达
- **任务管理**：今日任务一目了然
- **状态控制**：接单状态切换

### 1.2 功能架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                            工作台功能架构                                     │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                          工作台首页                                   │   │
│  │                                                                     │   │
│  │  ┌───────────────────────────────────────────────────────────────┐ │   │
│  │  │ 头部区域                                                       │ │   │
│  │  │ · 陪诊员信息（头像、姓名、等级）                                │ │   │
│  │  │ · 接单状态切换（休息中/接单中）                                 │ │   │
│  │  └───────────────────────────────────────────────────────────────┘ │   │
│  │                                                                     │   │
│  │  ┌───────────────────────────────────────────────────────────────┐ │   │
│  │  │ 统计卡片                                                       │ │   │
│  │  │ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐             │ │   │
│  │  │ │今日订单 │ │ 待服务  │ │ 已完成  │ │本月收入 │             │ │   │
│  │  │ │   3    │ │   2    │ │  128   │ │ ¥8,560 │             │ │   │
│  │  │ └─────────┘ └─────────┘ └─────────┘ └─────────┘             │ │   │
│  │  └───────────────────────────────────────────────────────────────┘ │   │
│  │                                                                     │   │
│  │  ┌───────────────────────────────────────────────────────────────┐ │   │
│  │  │ 快捷入口                                                       │ │   │
│  │  │ ┌─────────────────┐  ┌─────────────────┐                     │ │   │
│  │  │ │  ⚡ 抢单大厅     │  │  📋 我的订单    │                     │ │   │
│  │  │ │  5个新订单       │  │  查看全部订单   │                     │ │   │
│  │  │ └─────────────────┘  └─────────────────┘                     │ │   │
│  │  └───────────────────────────────────────────────────────────────┘ │   │
│  │                                                                     │   │
│  │  ┌───────────────────────────────────────────────────────────────┐ │   │
│  │  │ 今日任务                                          查看全部 ▶  │ │   │
│  │  │ ┌─────────────────────────────────────────────────────────┐ │ │   │
│  │  │ │ 09:00 │ 协和医院 │ 全程陪诊 │ 张先生      │ [待服务]    │ │ │   │
│  │  │ │ 14:00 │ 北医三院 │ 检查陪同 │ 李女士      │ [服务中]    │ │ │   │
│  │  │ └─────────────────────────────────────────────────────────┘ │ │   │
│  │  └───────────────────────────────────────────────────────────────┘ │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
│  功能子页面                                                                  │
│  ├── 抢单大厅 (/pages/workbench/orders/pool)                               │
│  ├── 订单详情 (/pages/workbench/orders/detail)                             │
│  ├── 收入明细 (/pages/workbench/earnings)        ← 规划中                  │
│  ├── 服务设置 (/pages/workbench/settings)        ← 规划中                  │
│  └── 操作规范 (/pages/workbench/guides)          ← 规划中                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 二、页面设计

### 2.1 工作台首页

```
┌─────────────────────────────────────────┐
│ 陪诊员工作台                     ⚙️     │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  [👤]  张晓红                    │   │
│  │        高级陪诊员               │   │
│  │                                  │   │
│  │  ┌─────────────────────────┐    │   │
│  │  │ ● 接单中              ▼ │    │   │
│  │  └─────────────────────────┘    │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 今日订单   待服务    已完成   本月收入│
│  │    3        2       128     ¥8,560│
│  └─────────────────────────────────┘   │
│                                         │
│  ┌───────────────┐ ┌───────────────┐   │
│  │ ⚡ 抢单大厅   │ │ 📋 我的订单   │   │
│  │  5个新订单    │ │  查看全部     │   │
│  └───────────────┘ └───────────────┘   │
│                                         │
│  今日任务                    查看全部 ▶ │
│  ┌─────────────────────────────────┐   │
│  │ 09:00 │ 协和医院           待服务│   │
│  │ 全程陪诊 · 张先生                 │   │
│  ├─────────────────────────────────┤   │
│  │ 14:00 │ 北医三院           服务中│   │
│  │ 检查陪同 · 李女士                 │   │
│  └─────────────────────────────────┘   │
│                                         │
│  更多功能                               │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐    │
│  │💰收入│ │📖规范│ │⚙️设置│ │📞客服│    │
│  │明细  │ │手册  │ │     │ │     │    │
│  └─────┘ └─────┘ └─────┘ └─────┘    │
│                                         │
└─────────────────────────────────────────┘
```

### 2.2 抢单大厅

```
┌─────────────────────────────────────────┐
│ ← 抢单大厅                       筛选 ▼ │
├─────────────────────────────────────────┤
│                                         │
│  当前有 5 个可抢订单                     │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 全程陪诊                         │   │
│  │ 北京协和医院 · 心内科             │   │
│  │                                  │   │
│  │ 📅 2024-12-20 09:00             │   │
│  │ 💰 ¥299 · 预计收入 ¥209          │   │
│  │                                  │   │
│  │ ┌─────────────────────────────┐ │   │
│  │ │          [立即抢单]          │ │   │
│  │ └─────────────────────────────┘ │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 检查陪同                         │   │
│  │ 北京大学第一医院 · 神经内科       │   │
│  │                                  │   │
│  │ 📅 2024-12-20 14:00             │   │
│  │ 💰 ¥199 · 预计收入 ¥139          │   │
│  │                                  │   │
│  │ ┌─────────────────────────────┐ │   │
│  │ │          [立即抢单]          │ │   │
│  │ └─────────────────────────────┘ │   │
│  └─────────────────────────────────┘   │
│                                         │
│  ⚠️ 抢单提示：                          │
│  · 请确保当日可提供服务再抢单           │
│  · 抢单成功后 30 分钟内未确认将自动取消 │
│                                         │
└─────────────────────────────────────────┘
```

### 2.3 订单详情（陪诊员视角）

```
┌─────────────────────────────────────────┐
│ ← 订单详情                              │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │       订单状态：待服务            │   │
│  │                                  │   │
│  │  ════════════════════════════   │   │
│  │  已接单 ──► 待服务 → 服务中 → 完成│   │
│  │    ✓         ●                  │   │
│  └─────────────────────────────────┘   │
│                                         │
│  服务信息                               │
│  ┌─────────────────────────────────┐   │
│  │ 服务类型   全程陪诊               │   │
│  │ 就诊医院   北京协和医院           │   │
│  │ 就诊科室   心内科                 │   │
│  │ 预约时间   2024-12-20 09:00      │   │
│  └─────────────────────────────────┘   │
│                                         │
│  就诊人信息                             │
│  ┌─────────────────────────────────┐   │
│  │ 姓名       张先生                 │   │
│  │ 性别       男                     │   │
│  │ 年龄       65岁                   │   │
│  │ 联系电话   138****1234    [拨打]  │   │
│  │ 备注       行动不便，需要轮椅     │   │
│  └─────────────────────────────────┘   │
│                                         │
│  用户信息                               │
│  ┌─────────────────────────────────┐   │
│  │ 下单人     王女士                 │   │
│  │ 联系电话   139****5678    [拨打]  │   │
│  └─────────────────────────────────┘   │
│                                         │
│  收入信息                               │
│  ┌─────────────────────────────────┐   │
│  │ 订单金额   ¥299                   │   │
│  │ 分成比例   70%                    │   │
│  │ 预计收入   ¥209.30                │   │
│  └─────────────────────────────────┘   │
│                                         │
├─────────────────────────────────────────┤
│                                         │
│  ┌─────────────────────────────────┐   │
│  │         [确认到达]               │   │
│  └─────────────────────────────────┘   │
│                                         │
└─────────────────────────────────────────┘
```

---

## 三、数据模型

### 3.1 统计数据结构

```typescript
interface EscortStats {
  // 今日数据
  todayOrders: number;         // 今日订单数
  todayEarnings: number;       // 今日收入
  
  // 待处理
  pendingOrders: number;       // 待服务订单数
  poolOrders: number;          // 抢单池订单数（接单中才显示）
  
  // 历史数据
  completedOrders: number;     // 已完成订单数
  totalEarnings: number;       // 累计收入
  
  // 本月数据
  monthOrders: number;         // 本月订单数
  monthEarnings: number;       // 本月收入
  
  // 评价数据
  rating: number;              // 当前评分
  ratingCount: number;         // 评价数量
}
```

### 3.2 工作状态

```prisma
model Escort {
  // ...
  workStatus     String    @default("resting")
  // resting: 休息中（不接收新订单）
  // working: 接单中（可以抢单）
  // busy: 服务中（正在执行服务，自动设置）
  // ...
}
```

---

## 四、业务逻辑

### 4.1 工作状态切换

```typescript
async function updateWorkStatus(
  userId: string, 
  status: 'working' | 'resting'
): Promise<{ success: boolean; status: string }> {
  const escort = await prisma.escort.findFirst({
    where: { userId }
  });
  
  if (!escort) {
    throw new NotFoundException('您不是陪诊员');
  }
  
  // 检查账号状态
  if (escort.status !== 'active') {
    throw new ForbiddenException('您的账号未激活，无法切换状态');
  }
  
  // 如果正在服务中，不允许切换
  if (escort.workStatus === 'busy') {
    throw new BadRequestException('您正在服务中，无法切换状态');
  }
  
  await prisma.escort.update({
    where: { id: escort.id },
    data: { workStatus: status }
  });
  
  return { success: true, status };
}
```

### 4.2 统计数据获取

```typescript
async function getEscortStats(userId: string): Promise<EscortStats> {
  const escort = await prisma.escort.findFirst({
    where: { userId }
  });
  
  if (!escort) {
    throw new NotFoundException('您不是陪诊员');
  }

  const now = new Date();
  const today = new Date(now.getFullYear(), now.getMonth(), now.getDate());
  const tomorrow = new Date(today);
  tomorrow.setDate(tomorrow.getDate() + 1);
  const startOfMonth = new Date(now.getFullYear(), now.getMonth(), 1);

  // 并行查询提高性能
  const [
    todayOrders,
    pendingOrders,
    completedOrders,
    monthOrders,
    poolOrders,
    earnings,
  ] = await Promise.all([
    // 今日订单
    prisma.order.count({
      where: {
        escortId: escort.id,
        appointmentDate: { gte: today, lt: tomorrow },
      },
    }),
    // 待服务订单
    prisma.order.count({
      where: {
        escortId: escort.id,
        status: { in: ['assigned', 'arrived'] },
      },
    }),
    // 已完成订单
    prisma.order.count({
      where: {
        escortId: escort.id,
        status: 'completed',
      },
    }),
    // 本月订单
    prisma.order.count({
      where: {
        escortId: escort.id,
        status: 'completed',
        completedAt: { gte: startOfMonth },
      },
    }),
    // 抢单池订单数
    escort.workStatus === 'working' 
      ? prisma.order.count({
          where: {
            status: 'paid',
            escortId: null,
          },
        })
      : 0,
    // 收入计算
    prisma.order.aggregate({
      where: {
        escortId: escort.id,
        status: 'completed',
        completedAt: { gte: startOfMonth },
      },
      _sum: { commissionAmount: true },
    }),
  ]);

  const monthEarnings = Number(earnings._sum.commissionAmount || 0);

  return {
    todayOrders,
    pendingOrders,
    completedOrders,
    monthOrders,
    monthEarnings: Math.round(monthEarnings * 100) / 100,
    poolOrders,
    rating: escort.rating,
    ratingCount: escort.ratingCount,
    // 其他字段...
  };
}
```

### 4.3 今日任务获取

```typescript
async function getTodayOrders(userId: string, limit = 10): Promise<Order[]> {
  const escort = await prisma.escort.findFirst({
    where: { userId }
  });
  
  if (!escort) {
    throw new NotFoundException('您不是陪诊员');
  }

  const today = new Date();
  today.setHours(0, 0, 0, 0);
  const tomorrow = new Date(today);
  tomorrow.setDate(tomorrow.getDate() + 1);

  return prisma.order.findMany({
    where: {
      escortId: escort.id,
      appointmentDate: {
        gte: today,
        lt: tomorrow,
      },
      status: { notIn: ['cancelled', 'refunded'] },
    },
    include: {
      service: { select: { id: true, name: true } },
      hospital: { select: { id: true, name: true, shortName: true } },
      patient: { select: { id: true, name: true, gender: true } },
    },
    orderBy: [
      { appointmentTime: 'asc' },
    ],
    take: limit,
  });
}
```

---

## 五、API 设计

### 5.1 工作台 API

| 方法 | 端点 | 说明 |
|------|------|------|
| GET | `/escort/profile` | 获取陪诊员信息 |
| GET | `/escort/stats` | 获取统计数据 |
| POST | `/escort/work-status` | 更新工作状态 |
| GET | `/escort/orders` | 获取订单列表 |
| GET | `/escort/orders/today` | 获取今日任务 |
| GET | `/escort/orders/pool` | 获取抢单池 |
| GET | `/escort/orders/:id` | 获取订单详情 |

### 5.2 接口响应示例

```typescript
// GET /escort/stats 响应
{
  "code": 0,
  "data": {
    "todayOrders": 3,
    "pendingOrders": 2,
    "completedOrders": 128,
    "monthOrders": 15,
    "monthEarnings": 8560.00,
    "poolOrders": 5,
    "rating": 4.9,
    "ratingCount": 120
  }
}

// GET /escort/orders/today 响应
{
  "code": 0,
  "data": [
    {
      "id": "xxx",
      "orderNo": "202412200001",
      "status": "assigned",
      "appointmentDate": "2024-12-20",
      "appointmentTime": "09:00",
      "service": { "id": "s1", "name": "全程陪诊" },
      "hospital": { "id": "h1", "name": "北京协和医院", "shortName": "协和" },
      "patient": { "id": "p1", "name": "张先生", "gender": "male" }
    }
  ]
}
```

---

## 六、前端实现

### 6.1 工作台首页组件结构

```tsx
// pages/workbench/index.tsx

export default function Workbench() {
  const { escortInfo, loading: identityLoading } = useEscortIdentity();
  const { data: stats, loading: statsLoading, refetch: refetchStats } = useEscortStats();
  const { data: todayOrders, refetch: refetchOrders } = useTodayOrders();
  
  // 页面显示时刷新
  useDidShow(() => {
    refetchStats();
    refetchOrders();
  });
  
  // 权限检查
  if (!isLoggedIn()) {
    return <LoginPrompt />;
  }
  
  if (identityLoading) {
    return <LoadingPage />;
  }
  
  if (!escortInfo) {
    return <NotEscortPage />;
  }
  
  return (
    <View className='workbench-page'>
      {/* 头部：陪诊员信息 + 状态切换 */}
      <WorkbenchHeader escort={escortInfo} />
      
      {/* 统计卡片 */}
      <StatsCard stats={stats} loading={statsLoading} />
      
      {/* 快捷入口 */}
      <QuickActions poolCount={stats?.poolOrders} />
      
      {/* 今日任务 */}
      <TodayTasks orders={todayOrders} />
      
      {/* 更多功能 */}
      <MoreFeatures />
    </View>
  );
}
```

### 6.2 状态切换组件

```tsx
function WorkStatusSwitch({ 
  currentStatus, 
  onChange 
}: {
  currentStatus: 'working' | 'resting' | 'busy';
  onChange: (status: 'working' | 'resting') => Promise<void>;
}) {
  const [loading, setLoading] = useState(false);
  
  const handleToggle = async () => {
    if (currentStatus === 'busy') {
      Taro.showToast({ title: '服务中无法切换', icon: 'none' });
      return;
    }
    
    const newStatus = currentStatus === 'working' ? 'resting' : 'working';
    
    try {
      setLoading(true);
      await onChange(newStatus);
      Taro.showToast({ 
        title: newStatus === 'working' ? '已开始接单' : '已停止接单',
        icon: 'success'
      });
    } catch (err) {
      Taro.showToast({ title: '操作失败', icon: 'none' });
    } finally {
      setLoading(false);
    }
  };
  
  const statusConfig = {
    working: { text: '接单中', color: '#52c41a', icon: 'check-circle' },
    resting: { text: '休息中', color: '#faad14', icon: 'pause-circle' },
    busy: { text: '服务中', color: '#1890ff', icon: 'clock' },
  };
  
  const config = statusConfig[currentStatus];
  
  return (
    <View 
      className={`work-status-switch ${currentStatus}`}
      onClick={handleToggle}
    >
      {loading ? (
        <View className='loading-spinner' />
      ) : (
        <>
          <Icon name={config.icon} size={16} color={config.color} />
          <Text style={{ color: config.color }}>{config.text}</Text>
          {currentStatus !== 'busy' && (
            <Icon name='chevron-down' size={12} color='#999' />
          )}
        </>
      )}
    </View>
  );
}
```

---

## 七、验收标准

### 基础功能
- [ ] 工作台正确显示陪诊员信息
- [ ] 统计数据实时准确
- [ ] 接单状态切换正常
- [ ] 今日任务列表正确显示

### 抢单功能
- [ ] 抢单大厅显示可抢订单
- [ ] 休息状态下抢单大厅为空
- [ ] 抢单成功后订单进入我的订单

### 交互体验
- [ ] 页面切换流畅
- [ ] 数据刷新及时
- [ ] 状态变化有明确反馈
- [ ] 错误提示友好

### 规划中功能
- [ ] 收入明细页面
- [ ] 服务设置页面
- [ ] 操作规范查看
