# UI 设计与交互规范

> 返回 [陪诊员中心总览](./README.md)  
> 状态：✅ 已规划  
> 版本：v1.1  
> 更新日期：2024-12-11

---

## 一、设计系统

### 1.1 设计原则

| 原则 | 说明 | 应用场景 |
|------|------|---------|
| **信任感** | 专业、可靠的视觉感受 | 证书展示、评分星级、完成单数 |
| **效率优先** | 减少操作步骤，突出关键信息 | 工作台一键抢单、状态快捷切换 |
| **状态清晰** | 明确的状态反馈和进度指示 | 订单状态、审核状态、工作状态 |
| **情感化** | 适度的激励和成就感 | 晋升进度、收入增长、好评展示 |

### 1.2 颜色系统

```scss
// 主色调（继承项目主题色）
$primary: var(--primary-color, #f97316);  // 橙色 - 品牌色
$primary-light: #fff7ed;                   // 橙色浅底

// 语义色
$success: #52c41a;       // 成功、在线、已完成
$warning: #faad14;       // 警告、审核中、休息中
$error: #ff4d4f;         // 错误、异常、封禁
$info: #1890ff;          // 信息、服务中

// 状态色应用
$status-colors: (
  // 陪诊员状态
  'active': $success,
  'pending': $warning,
  'inactive': #999,
  'suspended': $error,
  
  // 工作状态
  'working': $success,
  'resting': $warning,
  'busy': $info,
  
  // 订单状态
  'paid': $primary,
  'assigned': $info,
  'arrived': $info,
  'in_progress': $primary,
  'completed': $success,
  'cancelled': #999,
);

// 等级色
$level-colors: (
  'senior': #faad14,      // 金色 - 资深
  'intermediate': #1890ff, // 蓝色 - 中级
  'junior': #52c41a,      // 绿色 - 初级
  'trainee': #999,        // 灰色 - 实习
);

// 分销层级色
$distribution-colors: (
  'l1': #722ed1,  // 紫色 - 城市合伙人
  'l2': #eb2f96,  // 粉色 - 团队长
  'l3': $primary, // 橙色 - 陪诊员
);

// 中性色
$text-primary: #1f2937;
$text-secondary: #6b7280;
$text-tertiary: #9ca3af;
$border: #e5e7eb;
$background: #f5f7fa;
$card-bg: #ffffff;
```

### 1.3 间距系统

```scss
// 基础间距 (4px 倍数)
$spacing: (
  'xs': 4px,
  'sm': 8px,
  'md': 12px,
  'lg': 16px,
  'xl': 20px,
  'xxl': 24px,
  '3xl': 32px,
);

// 页面边距
$page-padding: 16px;

// 卡片间距
$card-padding: 16px;
$card-gap: 12px;
$card-radius: 12px;

// 组件间距
$section-gap: 20px;
$item-gap: 12px;
```

### 1.4 字体规范

```scss
// 字号
$font-size: (
  'xs': 10px,    // 辅助说明
  'sm': 12px,    // 次要信息
  'base': 14px,  // 正文
  'lg': 16px,    // 标题
  'xl': 18px,    // 大标题
  '2xl': 20px,   // 页面标题
  '3xl': 24px,   // 数字强调
  '4xl': 32px,   // 金额数字
);

// 字重
$font-weight: (
  'normal': 400,
  'medium': 500,
  'semibold': 600,
  'bold': 700,
);
```

---

## 二、终端预览组件扩展

### 2.1 新增页面类型

扩展现有 `terminal-preview` 组件，支持陪诊员相关页面：

```typescript
// types.ts 扩展
export type PreviewPageType = 
  | 'home'           // 首页
  | 'services'       // 服务列表
  | 'cases'          // 案例
  | 'profile'        // 个人中心
  // 新增陪诊员相关
  | 'escort-list'    // 陪诊员列表（用户端）
  | 'escort-detail'  // 陪诊员详情（用户端）
  | 'workbench'      // 工作台（陪诊员端）
  | 'order-pool'     // 抢单大厅（陪诊员端）
  | 'escort-orders'  // 订单列表（陪诊员端）
  | 'earnings'       // 收入明细（陪诊员端）
  | 'team'           // 团队管理（陪诊员端）
  | 'invite'         // 邀请页面（陪诊员端）
```

### 2.2 陪诊员类型定义

```typescript
// 陪诊员预览数据类型
export interface EscortPreviewItem {
  id: string;
  name: string;
  avatar?: string;
  level: 'senior' | 'intermediate' | 'junior' | 'trainee';
  levelName: string;
  rating: number;
  orderCount: number;
  experience?: string;
  introduction?: string;
  tags: string[];
  hospitals: Array<{
    id: string;
    name: string;
    familiarDepts?: string[];
  }>;
}

// 工作台统计数据
export interface WorkbenchStats {
  todayOrders: number;
  pendingOrders: number;
  completedOrders: number;
  monthEarnings: number;
  poolOrders: number;
  rating: number;
}

// 陪诊员端订单项
export interface EscortOrderItem {
  id: string;
  orderNo: string;
  status: string;
  statusText: string;
  service: {
    id: string;
    name: string;
  };
  hospital: {
    id: string;
    name: string;
    shortName?: string;
  };
  patient: {
    name: string;
    gender: string;
  };
  appointmentDate: string;
  appointmentTime: string;
  paidAmount: number;
  commissionAmount?: number;
}
```

### 2.3 新增组件结构

```
src/components/terminal-preview/
├── components/
│   ├── pages/
│   │   ├── EscortListPage.tsx      # 用户端 - 陪诊员列表
│   │   ├── EscortDetailPage.tsx    # 用户端 - 陪诊员详情
│   │   ├── WorkbenchPage.tsx       # 陪诊员端 - 工作台
│   │   ├── OrderPoolPage.tsx       # 陪诊员端 - 抢单大厅
│   │   ├── EscortOrdersPage.tsx    # 陪诊员端 - 订单列表
│   │   ├── EarningsPage.tsx        # 陪诊员端 - 收入明细
│   │   ├── TeamPage.tsx            # 陪诊员端 - 团队管理
│   │   └── InvitePage.tsx          # 陪诊员端 - 邀请页面
│   │
│   ├── escort/                      # 陪诊员相关子组件
│   │   ├── EscortCard.tsx          # 陪诊员卡片
│   │   ├── EscortRating.tsx        # 评分星级
│   │   ├── EscortTags.tsx          # 标签组
│   │   ├── LevelBadge.tsx          # 等级徽章
│   │   ├── WorkStatusSwitch.tsx    # 工作状态切换
│   │   ├── StatsGrid.tsx           # 统计网格
│   │   ├── OrderCard.tsx           # 订单卡片
│   │   ├── EarningsCard.tsx        # 收入卡片
│   │   └── TeamMemberCard.tsx      # 团队成员卡片
│   │
│   └── ... (现有组件)
```

---

## 三、页面组件设计

### 3.1 用户端 - 陪诊员卡片

```tsx
// EscortCard.tsx - 用于列表展示

interface EscortCardProps {
  escort: EscortPreviewItem;
  variant?: 'full' | 'compact' | 'horizontal';
  showActions?: boolean;
  onClick?: () => void;
}

/*
 ┌─────────────────────────────────────────┐
 │  ┌─────┐                                │
 │  │ 👤  │  张护士           [高级] 金色  │
 │  │     │  ★4.9 | 568单 | 5年经验       │
 │  └─────┘                                │
 │                                         │
 │  三甲医院护士出身，熟悉各大医院就诊...  │
 │                                         │
 │  [专业沟通] [耐心细致] [准时守约]       │
 │                                         │
 │  🏥 协和医院、北大医院                  │
 │                                         │
 │  variant='full' 时显示底部按钮:         │
 │  ┌─────────────────────────────────┐   │
 │  │           [立即预约]            │   │
 │  └─────────────────────────────────┘   │
 └─────────────────────────────────────────┘
*/
```

### 3.2 用户端 - 首页推荐区块

```tsx
// RecommendedEscorts.tsx - 首页推荐陪诊员

/*
 ┌─────────────────────────────────────────┐
 │ 优质陪诊员                   查看全部 ▶ │
 ├─────────────────────────────────────────┤
 │                                         │
 │  ┌─────────┐ ┌─────────┐ ┌─────────┐  │
 │  │  👤     │ │  👤     │ │  👤     │  │
 │  │ 张护士  │ │ 李护士  │ │ 王师傅  │  │
 │  │ ★4.9   │ │ ★4.8   │ │ ★4.7   │  │
 │  │ 568单  │ │ 423单  │ │ 312单  │  │
 │  │[立即约]│ │[立即约]│ │[立即约]│  │
 │  └─────────┘ └─────────┘ └─────────┘  │
 │                                         │
 │  横向滑动支持更多...                    │
 └─────────────────────────────────────────┘
*/
```

### 3.3 陪诊员端 - 工作台头部

```tsx
// WorkbenchHeader.tsx

interface WorkbenchHeaderProps {
  escort: {
    name: string;
    avatar?: string;
    level: string;
    levelName: string;
  };
  workStatus: 'working' | 'resting' | 'busy';
  onStatusChange: (status: 'working' | 'resting') => void;
}

/*
 ┌─────────────────────────────────────────┐
 │ ┌───────────────────────────────────┐   │
 │ │                                   │   │
 │ │  ┌─────┐  张晓红                  │   │
 │ │  │ 👤  │  高级陪诊员              │   │
 │ │  └─────┘                          │   │
 │ │                                   │   │
 │ │  ┌───────────────────────────┐   │   │
 │ │  │ ● 接单中              ▼  │   │   │
 │ │  └───────────────────────────┘   │   │
 │ │                                   │   │
 │ └───────────────────────────────────┘   │
 │                                         │
 │  背景渐变：$primary → transparent       │
 └─────────────────────────────────────────┘
*/
```

### 3.4 陪诊员端 - 统计卡片

```tsx
// WorkbenchStats.tsx

interface WorkbenchStatsProps {
  stats: WorkbenchStats;
  loading?: boolean;
}

/*
 ┌─────────────────────────────────────────┐
 │ ┌────────┬────────┬────────┬────────┐  │
 │ │今日订单│ 待服务 │ 已完成 │本月收入│  │
 │ │   3   │   2   │  128  │¥8,560 │  │
 │ │        │        │        │        │  │
 │ │ 较昨日 │ 需处理 │ 本月   │ 较上月 │  │
 │ │ +1    │        │        │ +12%  │  │
 │ └────────┴────────┴────────┴────────┘  │
 │                                         │
 │  点击可跳转对应详情                      │
 └─────────────────────────────────────────┘
*/
```

### 3.5 陪诊员端 - 订单卡片

```tsx
// EscortOrderCard.tsx

interface EscortOrderCardProps {
  order: EscortOrderItem;
  variant?: 'pool' | 'list' | 'detail';
  onAction?: (action: string) => void;
}

/*
 抢单池样式 (variant='pool'):
 ┌─────────────────────────────────────────┐
 │ 全程陪诊                                │
 │ 北京协和医院 · 心内科                   │
 │                                         │
 │ 📅 2024-12-20 09:00                    │
 │ 💰 ¥299 · 预计收入 ¥209                │
 │                                         │
 │ ┌─────────────────────────────────┐    │
 │ │          [立即抢单]             │    │
 │ └─────────────────────────────────┘    │
 └─────────────────────────────────────────┘
 
 订单列表样式 (variant='list'):
 ┌─────────────────────────────────────────┐
 │ 09:00 │ 协和医院              [待服务] │
 │ 全程陪诊 · 张先生                       │
 │                                         │
 │ 预计收入 ¥209                           │
 └─────────────────────────────────────────┘
*/
```

### 3.6 等级徽章组件

```tsx
// LevelBadge.tsx

interface LevelBadgeProps {
  level: 'senior' | 'intermediate' | 'junior' | 'trainee';
  size?: 'sm' | 'md' | 'lg';
  showIcon?: boolean;
}

/*
 高级 [👑 高级]  - 金色背景
 中级 [🔷 中级]  - 蓝色背景
 初级 [🟢 初级]  - 绿色背景
 实习 [○ 实习]   - 灰色背景
*/
```

### 3.7 分销层级徽章

```tsx
// DistributionBadge.tsx

interface DistributionBadgeProps {
  level: 1 | 2 | 3;
  showIcon?: boolean;
}

/*
 L1 [🏆 城市合伙人]  - 紫色
 L2 [⭐ 团队长]      - 粉色
 L3 [● 陪诊员]      - 橙色
*/
```

---

## 四、交互状态设计

### 4.1 加载状态

```tsx
// 骨架屏设计
/*
 陪诊员卡片骨架:
 ┌─────────────────────────────────────────┐
 │  ┌─────┐  ▓▓▓▓▓▓▓▓▓    ▓▓▓▓           │
 │  │░░░░░│  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓           │
 │  └─────┘                                │
 │                                         │
 │  ▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓▓  │
 │                                         │
 │  [▓▓▓▓] [▓▓▓▓] [▓▓▓▓]                  │
 └─────────────────────────────────────────┘
 
 工作台骨架:
 ┌─────────────────────────────────────────┐
 │  ┌─────┐  ▓▓▓▓▓▓▓                       │
 │  │░░░░░│  ▓▓▓▓▓▓▓▓▓                     │
 │  └─────┘                                │
 ├─────────────────────────────────────────┤
 │  ┌────────┬────────┬────────┬────────┐ │
 │  │░░░░░░░░│░░░░░░░░│░░░░░░░░│░░░░░░░░│ │
 │  └────────┴────────┴────────┴────────┘ │
 └─────────────────────────────────────────┘
*/

// 组件实现
export function EscortCardSkeleton() {
  return (
    <div className="escort-card animate-pulse">
      <div className="flex gap-3">
        <div className="w-16 h-16 bg-gray-200 rounded-full" />
        <div className="flex-1">
          <div className="h-4 bg-gray-200 rounded w-24 mb-2" />
          <div className="h-3 bg-gray-200 rounded w-32" />
        </div>
      </div>
      <div className="h-3 bg-gray-200 rounded w-full mt-3" />
      <div className="flex gap-2 mt-3">
        <div className="h-6 bg-gray-200 rounded w-16" />
        <div className="h-6 bg-gray-200 rounded w-16" />
      </div>
    </div>
  );
}
```

### 4.2 空状态

```tsx
// 空状态设计
interface EmptyStateProps {
  type: 'no-escorts' | 'no-orders' | 'no-pool' | 'no-team' | 'no-earnings';
  action?: {
    text: string;
    onClick: () => void;
  };
}

/*
 无陪诊员:
 ┌─────────────────────────────────────────┐
 │                                         │
 │              🔍                         │
 │                                         │
 │        暂无符合条件的陪诊员              │
 │        试试调整筛选条件                  │
 │                                         │
 │        [重置筛选]                        │
 │                                         │
 └─────────────────────────────────────────┘
 
 抢单池为空:
 ┌─────────────────────────────────────────┐
 │                                         │
 │              ☕                         │
 │                                         │
 │           暂无可抢订单                   │
 │        休息一下，稍后再来                │
 │                                         │
 │  💡 提示：保持「接单中」状态可收到推送   │
 │                                         │
 └─────────────────────────────────────────┘
 
 无团队成员:
 ┌─────────────────────────────────────────┐
 │                                         │
 │              👥                         │
 │                                         │
 │           暂无团队成员                   │
 │        邀请好友加入，赚取分润            │
 │                                         │
 │        [立即邀请]                        │
 │                                         │
 └─────────────────────────────────────────┘
*/
```

### 4.3 错误状态

```tsx
// 错误状态设计
interface ErrorStateProps {
  type: 'network' | 'server' | 'permission' | 'not-found';
  message?: string;
  onRetry?: () => void;
}

/*
 网络错误:
 ┌─────────────────────────────────────────┐
 │                                         │
 │              📡                         │
 │                                         │
 │           网络连接失败                   │
 │        请检查网络后重试                  │
 │                                         │
 │        [重新加载]                        │
 │                                         │
 └─────────────────────────────────────────┘
 
 权限不足（非陪诊员访问工作台）:
 ┌─────────────────────────────────────────┐
 │                                         │
 │              🔒                         │
 │                                         │
 │           您还不是陪诊员                 │
 │        成为陪诊员，开启副业之旅          │
 │                                         │
 │        [了解更多]                        │
 │                                         │
 └─────────────────────────────────────────┘
*/
```

### 4.4 操作反馈

```tsx
// Toast 提示
const toastConfig = {
  success: {
    'order-grabbed': '抢单成功！请及时联系用户',
    'status-changed': '状态已更新',
    'withdraw-applied': '提现申请已提交',
  },
  error: {
    'order-taken': '订单已被抢走',
    'insufficient-balance': '余额不足',
    'network-error': '网络异常，请重试',
  },
  loading: {
    'grabbing': '抢单中...',
    'submitting': '提交中...',
  },
};

// 确认弹窗
interface ConfirmDialogProps {
  type: 'grab-order' | 'change-status' | 'withdraw';
  data?: any;
  onConfirm: () => void;
  onCancel: () => void;
}

/*
 抢单确认:
 ┌─────────────────────────────────────────┐
 │                                         │
 │        确认抢单？                        │
 │                                         │
 │  服务：全程陪诊                          │
 │  医院：北京协和医院                      │
 │  时间：2024-12-20 09:00                 │
 │  预计收入：¥209                          │
 │                                         │
 │  ⚠️ 抢单后请务必按时到达                 │
 │                                         │
 │        [取消]      [确认抢单]            │
 │                                         │
 └─────────────────────────────────────────┘
*/
```

---

## 五、用户旅程

### 5.1 用户端 - 选择陪诊员

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       用户选择陪诊员旅程                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  入口                                                                       │
│  ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐                    │
│  │ 首页推荐│   │金刚区入口│   │服务详情 │   │医院详情 │                    │
│  │         │   │         │   │选择陪诊│   │熟悉本院│                    │
│  └────┬────┘   └────┬────┘   └────┬────┘   └────┬────┘                    │
│       │             │             │             │                          │
│       └─────────────┴─────────────┴─────────────┘                          │
│                            │                                                │
│                            ▼                                                │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                       陪诊员列表页                                   │   │
│  │                                                                     │   │
│  │  筛选条件：好评优先 / 接单量 / 等级                                  │   │
│  │  卡片展示：头像、姓名、等级、评分、订单数、标签、医院                 │   │
│  │                                                                     │   │
│  └────────────────────────────┬────────────────────────────────────────┘   │
│                               │ 点击卡片                                    │
│                               ▼                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                       陪诊员详情页                                   │   │
│  │                                                                     │   │
│  │  信息展示：完整资料、证书、熟悉医院及科室、用户评价                   │   │
│  │  行动按钮：[咨询] [立即预约]                                         │   │
│  │                                                                     │   │
│  └────────────────────────────┬────────────────────────────────────────┘   │
│                               │ 点击预约                                    │
│                               ▼                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                       预约下单页                                     │   │
│  │                                                                     │   │
│  │  已选陪诊员展示 + 可更换                                             │   │
│  │  填写就诊信息 → 提交订单 → 支付                                      │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 陪诊员端 - 接单服务流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       陪诊员接单服务旅程                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  准备阶段                                                                    │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  个人中心 → 工作台入口 → 工作台首页                                   │   │
│  │                          ┌───────────────┐                          │   │
│  │                          │ 切换为「接单中」│                          │   │
│  │                          └───────┬───────┘                          │   │
│  └──────────────────────────────────┼──────────────────────────────────┘   │
│                                     │                                       │
│  接单阶段                            ▼                                       │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │  方式1：抢单大厅                   方式2：消息推送                    │   │
│  │  ┌───────────────┐              ┌───────────────┐                  │   │
│  │  │ 查看订单池    │              │ 收到新订单推送│                  │   │
│  │  │ 选择订单      │              │ 点击查看      │                  │   │
│  │  │ 确认抢单      │              │ 确认抢单      │                  │   │
│  │  └───────┬───────┘              └───────┬───────┘                  │   │
│  │          │                              │                          │   │
│  │          └──────────────┬───────────────┘                          │   │
│  │                         │ 抢单成功                                  │   │
│  │                         ▼                                          │   │
│  │               ┌───────────────────┐                                │   │
│  │               │ 订单进入「待服务」 │                                │   │
│  │               └─────────┬─────────┘                                │   │
│  └─────────────────────────┼───────────────────────────────────────────┘   │
│                            │                                               │
│  服务阶段                   ▼                                               │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐        │   │
│  │  │ 查看详情│───►│ 确认到达│───►│ 开始服务│───►│ 完成服务│        │   │
│  │  │ 联系用户│    │         │    │ 状态→busy│   │         │        │   │
│  │  └─────────┘    └─────────┘    └─────────┘    └────┬────┘        │   │
│  │                                                     │              │   │
│  └─────────────────────────────────────────────────────┼──────────────┘   │
│                                                        │                   │
│  结算阶段                                               ▼                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                                                                     │   │
│  │  ┌───────────────┐    ┌───────────────┐    ┌───────────────┐      │   │
│  │  │ 分成自动入账   │───►│ 查看收入明细  │───►│ 申请提现      │      │   │
│  │  │ 通知用户评价   │    │               │    │               │      │   │
│  │  └───────────────┘    └───────────────┘    └───────────────┘      │   │
│  │                                                                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.3 陪诊员端 - 晋升旅程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       陪诊员晋升旅程                                         │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                      L3 普通陪诊员                                   │   │
│  │                                                                     │   │
│  │  核心任务：                                                          │   │
│  │  ☑ 完成订单积累经验 (目标50单)                                       │   │
│  │  ☑ 保持高评分 (≥4.5)                                                │   │
│  │  ☑ 邀请好友加入 (3人)                                               │   │
│  │                                                                     │   │
│  │  触点：                                                              │   │
│  │  · 工作台首页显示晋升进度                                            │   │
│  │  · 完成关键里程碑时推送通知                                          │   │
│  │  · 邀请页面突出展示分润收益                                          │   │
│  └────────────────────────────┬────────────────────────────────────────┘   │
│                               │ 满足条件                                    │
│                               ▼                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                      自动晋升到 L2 团队长                            │   │
│  │                                                                     │   │
│  │  新增权限：                                                          │   │
│  │  · 查看团队成员数据                                                  │   │
│  │  · 团队订单分润 3%                                                   │   │
│  │  · 团队管理工具                                                      │   │
│  │                                                                     │   │
│  │  核心任务：                                                          │   │
│  │  ☑ 发展团队 (目标10人)                                              │   │
│  │  ☑ 团队月订单 (100+)                                                │   │
│  │  ☑ 个人月订单 (30+)                                                 │   │
│  └────────────────────────────┬────────────────────────────────────────┘   │
│                               │ 满足条件+平台审核                           │
│                               ▼                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                      晋升为 L1 城市合伙人                            │   │
│  │                                                                     │   │
│  │  特殊权限：                                                          │   │
│  │  · 区域管理权限                                                      │   │
│  │  · 审核团队长                                                        │   │
│  │  · 区域订单分润 2%                                                   │   │
│  │  · 专属数据看板                                                      │   │
│  │  · 优先获取平台资源                                                  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 六、动效规范

### 6.1 转场动画

```scss
// 页面切换
.page-transition {
  &-enter {
    opacity: 0;
    transform: translateX(20px);
  }
  &-enter-active {
    opacity: 1;
    transform: translateX(0);
    transition: all 0.3s ease-out;
  }
  &-exit {
    opacity: 1;
  }
  &-exit-active {
    opacity: 0;
    transition: opacity 0.2s ease-in;
  }
}

// 弹窗
.modal-transition {
  &-enter {
    opacity: 0;
    transform: scale(0.95);
  }
  &-enter-active {
    opacity: 1;
    transform: scale(1);
    transition: all 0.2s ease-out;
  }
}
```

### 6.2 微交互

```scss
// 按钮点击
.btn-press {
  transition: transform 0.1s;
  &:active {
    transform: scale(0.98);
  }
}

// 状态切换
.status-switch {
  transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
}

// 抢单成功动画
@keyframes grab-success {
  0% { transform: scale(1); }
  50% { transform: scale(1.05); }
  100% { transform: scale(1); }
}

.grab-success {
  animation: grab-success 0.3s ease-out;
}

// 收入入账动画
@keyframes income-in {
  0% { 
    opacity: 0; 
    transform: translateY(-10px); 
  }
  100% { 
    opacity: 1; 
    transform: translateY(0); 
  }
}
```

---

## 七、终端预览集成

### 7.1 工作台预览组件

```tsx
// 在管理后台使用，实时预览陪诊员工作台效果

import { TerminalPreview } from '@/components/terminal-preview';

export function WorkbenchPreview() {
  return (
    <TerminalPreview
      page="workbench"
      mockData={{
        escort: {
          name: '张晓红',
          level: 'senior',
          workStatus: 'working',
        },
        stats: {
          todayOrders: 3,
          pendingOrders: 2,
          monthEarnings: 8560,
          poolOrders: 5,
        },
        todayOrders: [...],
      }}
    />
  );
}
```

### 7.2 陪诊员列表预览

```tsx
// 在首页配置时预览推荐陪诊员效果

import { TerminalPreview } from '@/components/terminal-preview';

export function EscortRecommendPreview() {
  return (
    <TerminalPreview
      page="home"
      showEscortSection={true}
      escorts={[
        { id: '1', name: '张护士', rating: 4.9, orderCount: 568 },
        { id: '2', name: '李护士', rating: 4.8, orderCount: 423 },
      ]}
    />
  );
}
```

---

## 八、验收标准

### 设计系统
- [ ] 颜色系统符合品牌规范
- [ ] 间距、字体统一
- [ ] 状态色应用正确

### 组件实现
- [ ] 陪诊员卡片组件完成
- [ ] 等级徽章组件完成
- [ ] 工作台组件完成
- [ ] 订单卡片组件完成

### 交互状态
- [ ] 骨架屏正确显示
- [ ] 空状态友好
- [ ] 错误状态有引导
- [ ] 操作反馈及时

### 终端预览
- [ ] 支持工作台页面预览
- [ ] 支持陪诊员列表预览
- [ ] 支持陪诊员详情预览
- [ ] 数据可配置覆盖
- [ ] 深色模式显示正常
- [ ] 无障碍功能完整

---

## 九、深色模式支持

### 9.1 颜色变量定义

```scss
// 深色模式颜色变量
:root {
  // 亮色模式（默认）
  --color-bg-primary: #ffffff;
  --color-bg-secondary: #f5f7fa;
  --color-bg-tertiary: #f0f2f5;
  --color-bg-card: #ffffff;
  
  --color-text-primary: #1f2937;
  --color-text-secondary: #6b7280;
  --color-text-tertiary: #9ca3af;
  --color-text-disabled: #d1d5db;
  
  --color-border: #e5e7eb;
  --color-border-light: #f3f4f6;
  
  --color-shadow: rgba(0, 0, 0, 0.1);
}

// 深色模式
@media (prefers-color-scheme: dark) {
  :root {
    --color-bg-primary: #0f172a;
    --color-bg-secondary: #1e293b;
    --color-bg-tertiary: #334155;
    --color-bg-card: #1e293b;
    
    --color-text-primary: #f1f5f9;
    --color-text-secondary: #94a3b8;
    --color-text-tertiary: #64748b;
    --color-text-disabled: #475569;
    
    --color-border: #334155;
    --color-border-light: #1e293b;
    
    --color-shadow: rgba(0, 0, 0, 0.5);
  }
}

// 小程序端需要通过 JS 检测并添加类名
.dark-mode {
  --color-bg-primary: #0f172a;
  --color-bg-secondary: #1e293b;
  // ... 同上
}

// 主题色在深色模式下的调整
@media (prefers-color-scheme: dark) {
  :root {
    // 主色稍微提亮以保证对比度
    --primary-color: #fb923c;
    --primary-light: rgba(249, 115, 22, 0.15);
    
    // 语义色调整
    --success-color: #4ade80;
    --warning-color: #fbbf24;
    --error-color: #f87171;
    --info-color: #60a5fa;
  }
}
```

### 9.2 组件深色模式适配

```scss
// 卡片组件
.escort-card {
  background: var(--color-bg-card);
  border: 1px solid var(--color-border);
  border-radius: 12px;
  box-shadow: 0 2px 8px var(--color-shadow);
  
  .escort-name {
    color: var(--color-text-primary);
  }
  
  .escort-stats {
    color: var(--color-text-secondary);
  }
  
  .escort-tags .tag {
    background: var(--color-bg-tertiary);
    color: var(--color-text-secondary);
  }
}

// 工作台统计卡片
.stats-card {
  background: linear-gradient(
    135deg, 
    var(--primary-color) 0%, 
    var(--primary-light) 100%
  );
  
  @media (prefers-color-scheme: dark) {
    background: linear-gradient(
      135deg,
      rgba(249, 115, 22, 0.8) 0%,
      rgba(249, 115, 22, 0.2) 100%
    );
  }
}

// 订单卡片
.order-card {
  background: var(--color-bg-card);
  
  .order-status {
    &.completed { color: var(--success-color); }
    &.in-progress { color: var(--info-color); }
    &.pending { color: var(--warning-color); }
  }
}

// 按钮
.btn-primary {
  background: var(--primary-color);
  color: #ffffff;
  
  &:active {
    background: var(--primary-dark);
  }
}

.btn-secondary {
  background: var(--color-bg-tertiary);
  color: var(--color-text-primary);
  border: 1px solid var(--color-border);
}
```

### 9.3 小程序深色模式检测

```typescript
// miniapp/src/utils/theme.ts

import Taro from '@tarojs/taro';
import { useState, useEffect } from 'react';

// 检测系统主题
export function useSystemTheme(): 'light' | 'dark' {
  const [theme, setTheme] = useState<'light' | 'dark'>('light');
  
  useEffect(() => {
    // 初始检测
    try {
      const systemInfo = Taro.getSystemInfoSync();
      setTheme(systemInfo.theme || 'light');
    } catch (e) {
      setTheme('light');
    }
    
    // 监听主题变化
    const handler = (res: { theme: 'light' | 'dark' }) => {
      setTheme(res.theme);
    };
    
    Taro.onThemeChange(handler);
    
    return () => {
      Taro.offThemeChange(handler);
    };
  }, []);
  
  return theme;
}

// 在 App 组件中使用
export default function App({ children }) {
  const theme = useSystemTheme();
  
  return (
    <View className={`app-container ${theme === 'dark' ? 'dark-mode' : ''}`}>
      {children}
    </View>
  );
}
```

---

## 十、无障碍设计规范

### 10.1 基础要求

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         无障碍设计核心原则                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  WCAG 2.1 AA 级别要求                                                        │
│  ─────────────────────                                                      │
│                                                                             │
│  1. 可感知 (Perceivable)                                                    │
│     · 文本与背景对比度 ≥ 4.5:1                                              │
│     · 大文本对比度 ≥ 3:1                                                    │
│     · 图片提供替代文本                                                      │
│     · 不单独依赖颜色传达信息                                                │
│                                                                             │
│  2. 可操作 (Operable)                                                       │
│     · 所有功能可键盘操作                                                    │
│     · 触摸目标 ≥ 44×44 像素                                                 │
│     · 提供足够的操作时间                                                    │
│     · 避免闪烁内容                                                          │
│                                                                             │
│  3. 可理解 (Understandable)                                                 │
│     · 使用清晰简洁的语言                                                    │
│     · 表单有明确的标签和提示                                                │
│     · 错误信息明确且提供解决建议                                            │
│                                                                             │
│  4. 健壮性 (Robust)                                                         │
│     · 兼容辅助技术（屏幕阅读器）                                            │
│     · 使用语义化标签                                                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 10.2 颜色对比度

```typescript
// 对比度检测工具
function getContrastRatio(color1: string, color2: string): number {
  const lum1 = getLuminance(color1);
  const lum2 = getLuminance(color2);
  const lighter = Math.max(lum1, lum2);
  const darker = Math.min(lum1, lum2);
  return (lighter + 0.05) / (darker + 0.05);
}

function getLuminance(color: string): number {
  const rgb = hexToRgb(color);
  const [r, g, b] = [rgb.r, rgb.g, rgb.b].map(v => {
    v /= 255;
    return v <= 0.03928 ? v / 12.92 : Math.pow((v + 0.055) / 1.055, 2.4);
  });
  return 0.2126 * r + 0.7152 * g + 0.0722 * b;
}

// 设计系统中的对比度验证
const colorPairs = [
  { fg: '#1f2937', bg: '#ffffff', expected: 13.1 },  // 主文字/白底 ✅
  { fg: '#6b7280', bg: '#ffffff', expected: 5.3 },   // 次要文字/白底 ✅
  { fg: '#f97316', bg: '#ffffff', expected: 3.0 },   // 主色/白底 (大文本可用)
  { fg: '#f1f5f9', bg: '#1e293b', expected: 10.8 },  // 深色模式主文字 ✅
];
```

### 10.3 触摸目标尺寸

```scss
// 确保所有可点击元素足够大
.touchable {
  min-width: 44px;
  min-height: 44px;
  
  // 对于小图标，使用 padding 扩大点击区域
  &.icon-btn {
    padding: 12px;
    
    .icon {
      width: 20px;
      height: 20px;
    }
  }
}

// 列表项
.list-item {
  min-height: 48px;
  padding: 12px 16px;
  
  // 可点击区域覆盖整行
  &.clickable {
    cursor: pointer;
    
    &:active {
      background: var(--color-bg-tertiary);
    }
  }
}

// 按钮
.btn {
  min-height: 44px;
  padding: 0 24px;
  
  &.btn-small {
    min-height: 36px;
    padding: 0 16px;
    font-size: 14px;
  }
  
  &.btn-large {
    min-height: 52px;
    padding: 0 32px;
    font-size: 18px;
  }
}
```

### 10.4 焦点状态

```scss
// 全局焦点样式
*:focus-visible {
  outline: 2px solid var(--primary-color);
  outline-offset: 2px;
}

// 自定义焦点样式
.custom-focus {
  &:focus {
    outline: none;
  }
  
  &:focus-visible {
    box-shadow: 0 0 0 3px var(--primary-light);
    border-color: var(--primary-color);
  }
}

// 输入框焦点
.input-field {
  border: 1px solid var(--color-border);
  transition: border-color 0.2s, box-shadow 0.2s;
  
  &:focus {
    border-color: var(--primary-color);
    box-shadow: 0 0 0 3px var(--primary-light);
  }
}
```

### 10.5 屏幕阅读器支持

```tsx
// 语义化组件示例

// 陪诊员卡片
function EscortCard({ escort }: { escort: Escort }) {
  return (
    <View 
      className='escort-card'
      role='article'
      aria-label={`陪诊员 ${escort.name}，${escort.levelName}，评分 ${escort.rating} 分，完成 ${escort.orderCount} 单`}
    >
      <Image 
        src={escort.avatar} 
        className='avatar'
        aria-hidden='true'  // 装饰性图片对屏幕阅读器隐藏
      />
      <View className='info'>
        <Text className='name'>{escort.name}</Text>
        <Text className='level' aria-label={`等级：${escort.levelName}`}>
          {escort.levelName}
        </Text>
        <View className='stats' aria-label={`评分 ${escort.rating} 分，完成 ${escort.orderCount} 单`}>
          <Text>★{escort.rating}</Text>
          <Text>{escort.orderCount}单</Text>
        </View>
      </View>
      <Button 
        className='book-btn'
        aria-label={`预约 ${escort.name}`}
      >
        立即预约
      </Button>
    </View>
  );
}

// 订单状态
function OrderStatus({ status }: { status: string }) {
  const statusMap = {
    pending: { text: '待支付', color: 'warning' },
    paid: { text: '待接单', color: 'info' },
    assigned: { text: '已派单', color: 'info' },
    in_progress: { text: '服务中', color: 'primary' },
    completed: { text: '已完成', color: 'success' },
    cancelled: { text: '已取消', color: 'disabled' },
  };
  
  const config = statusMap[status];
  
  return (
    <View 
      className={`order-status ${config.color}`}
      role='status'
      aria-live='polite'
      aria-label={`订单状态：${config.text}`}
    >
      {config.text}
    </View>
  );
}

// 工作状态切换
function WorkStatusSwitch({ status, onChange }) {
  return (
    <View 
      className='work-status-switch'
      role='switch'
      aria-checked={status === 'working'}
      aria-label={`接单状态，当前${status === 'working' ? '接单中' : '休息中'}`}
      onClick={() => onChange(status === 'working' ? 'resting' : 'working')}
    >
      <Text className='status-text'>
        {status === 'working' ? '接单中' : '休息中'}
      </Text>
    </View>
  );
}
```

### 10.6 表单无障碍

```tsx
// 带标签和错误提示的输入框
function FormField({ 
  label, 
  name, 
  type = 'text',
  required = false,
  error,
  hint,
  value,
  onChange,
}: FormFieldProps) {
  const inputId = `field-${name}`;
  const hintId = hint ? `${inputId}-hint` : undefined;
  const errorId = error ? `${inputId}-error` : undefined;
  
  return (
    <View className='form-field'>
      <Label 
        htmlFor={inputId}
        className={`field-label ${required ? 'required' : ''}`}
      >
        {label}
        {required && <Text aria-hidden='true'> *</Text>}
      </Label>
      
      {hint && (
        <Text id={hintId} className='field-hint'>
          {hint}
        </Text>
      )}
      
      <Input
        id={inputId}
        name={name}
        type={type}
        value={value}
        onChange={onChange}
        className={`field-input ${error ? 'has-error' : ''}`}
        aria-required={required}
        aria-describedby={[hintId, errorId].filter(Boolean).join(' ') || undefined}
        aria-invalid={!!error}
      />
      
      {error && (
        <Text 
          id={errorId} 
          className='field-error'
          role='alert'
          aria-live='assertive'
        >
          {error}
        </Text>
      )}
    </View>
  );
}

// 提现表单示例
function WithdrawForm() {
  const [amount, setAmount] = useState('');
  const [error, setError] = useState('');
  
  return (
    <Form
      aria-labelledby='withdraw-title'
      onSubmit={handleSubmit}
    >
      <Text id='withdraw-title' className='form-title'>
        申请提现
      </Text>
      
      <FormField
        label='提现金额'
        name='amount'
        type='number'
        required
        hint='最低提现金额 ¥100'
        error={error}
        value={amount}
        onChange={(e) => setAmount(e.target.value)}
      />
      
      <Button 
        type='submit'
        className='btn-primary'
        disabled={!amount}
        aria-busy={submitting}
      >
        {submitting ? '提交中...' : '确认提现'}
      </Button>
    </Form>
  );
}
```

### 10.7 动效与动画

```scss
// 减少动画（根据用户偏好）
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
    scroll-behavior: auto !important;
  }
}

// 默认动画
.fade-in {
  animation: fadeIn 0.3s ease-out;
  
  @media (prefers-reduced-motion: reduce) {
    animation: none;
    opacity: 1;
  }
}

@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}
```

---

## 十一、变更记录

| 版本 | 日期 | 变更内容 | 作者 |
|------|------|---------|------|
| v1.0 | 2024-12-11 | 初始版本 | - |
| v1.1 | 2024-12-11 | 新增深色模式支持、无障碍设计规范 | - |
