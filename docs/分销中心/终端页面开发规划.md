# 分销中心终端页面开发规划

> **文档版本**: v1.0  
> **创建日期**: 2024-12-12  
> **最后更新**: 2024-12-12  
> **状态**: 📋 规划中  
> **返回**: [代码审计报告](./代码审计报告-2024-12-12.md)

---

## 一、背景与目标

### 1.1 开发策略

采用"终端预览器优先"策略：

```
┌─────────────────────────────────────────────────────────────┐
│                     开发流程                                  │
├─────────────────────────────────────────────────────────────┤
│  1. 终端预览器 (TerminalPreview)                              │
│     ├── 在 Web 管理后台中模拟手机终端                          │
│     ├── 快速开发、调试、测试                                   │
│     └── 与后台联动，实时预览                                   │
│                     ↓                                        │
│  2. 功能验证通过后                                            │
│                     ↓                                        │
│  3. 迁移到真正的小程序 (miniapp/)                             │
└─────────────────────────────────────────────────────────────┘
```

### 1.2 分销系统概述

分销系统是陪诊员生态的核心增长模块，支持三级分销模型：

- **L1 (高级合伙人)**: 最高等级，可获取三层下线分润
- **L2 (合伙人)**: 中级等级，可获取两层下线分润
- **L3 (普通陪诊员)**: 初始等级，可获取直属下线分润

### 1.3 开发目标

在终端预览器中实现以下功能页面：

1. **我的团队首页** - 团队统计 + 邀请入口
2. **团队成员列表** - 直属成员管理
3. **分润记录** - 收益流水明细
4. **晋升中心** - 等级晋升进度

---

## 二、页面规划清单

### 2.1 终端页面清单

| 页面路径 | 路由 Key | 功能说明 | 优先级 | 状态 |
|---------|---------|---------|--------|------|
| `/pages/distribution/team/index` | `distribution-team` | 我的团队首页 | **P0** | ❌ 待开发 |
| `/pages/distribution/team/members` | `distribution-members` | 团队成员列表 | **P0** | ❌ 待开发 |
| `/pages/distribution/records/index` | `distribution-records` | 分润记录 | **P0** | ❌ 待开发 |
| `/pages/distribution/promotion/index` | `distribution-promotion` | 晋升中心 | P1 | ❌ 待开发 |
| `/pages/distribution/invite/index` | `distribution-invite` | 邀请好友(海报) | P2 | ❌ 待开发 |

### 2.2 预览器页面组件清单

| 组件文件 | 路径 | 优先级 | 状态 | 说明 |
|---------|------|--------|------|------|
| `DistributionTeamPage.tsx` | `components/pages/distribution/` | P0 | ❌ 待开发 | 团队首页 |
| `DistributionMembersPage.tsx` | `components/pages/distribution/` | P0 | ❌ 待开发 | 成员列表 |
| `DistributionRecordsPage.tsx` | `components/pages/distribution/` | P0 | ❌ 待开发 | 分润记录 |
| `DistributionPromotionPage.tsx` | `components/pages/distribution/` | P1 | ❌ 待开发 | 晋升中心 |
| `DistributionInvitePage.tsx` | `components/pages/distribution/` | P2 | ❌ 待开发 | 邀请海报 |

---

## 三、后端 API 接入清单

### 3.1 已有 API（可直接接入）

| API 方法 | 端点 | 请求通道 | 说明 |
|---------|------|---------|------|
| `getInviteCode()` | `GET /escort/distribution/invite-code` | **escortRequest** | 获取/生成邀请码 |
| `processInvitation()` | `POST /escort/distribution/invite` | **escortRequest** | 使用邀请码绑定关系 |
| `getDistributionRecords()` | `GET /escort/distribution/records` | **escortRequest** | 获取分润记录(分页) |
| `getTeamMembers()` | `GET /escort/team/members` | **escortRequest** | 获取团队成员(分页) |
| `getTeamStats()` | `GET /escort/team/stats` | **escortRequest** | 获取团队统计数据 |
| `getPromotionStatus()` | `GET /escort/promotion/status` | **escortRequest** | 获取晋升状态 |
| `applyPromotion()` | `POST /escort/promotion/apply` | **escortRequest** | 申请晋升 |

### 3.2 previewApi 扩展计划

```typescript
// api.ts 新增

// ==========================================================================
// 分销中心（Escort Channel）
// ==========================================================================

/**
 * 获取邀请码
 * 接口: GET /escort/distribution/invite-code
 * 通道: escortRequest
 */
getDistributionInviteCode: async (): Promise<InviteCodeResponse> => {
  try {
    return await escortRequest<InviteCodeResponse>('/escort/distribution/invite-code')
  } catch (error) {
    if (error instanceof ApiError && (error.status === 404 || error.status === 500)) {
      console.warn('[previewApi.getDistributionInviteCode] 使用 mock 数据')
      return getMockInviteCodeData()
    }
    throw error
  }
},

/**
 * 获取团队统计
 * 接口: GET /escort/team/stats
 * 通道: escortRequest
 */
getTeamStats: async (): Promise<TeamStatsResponse> => {
  try {
    return await escortRequest<TeamStatsResponse>('/escort/team/stats')
  } catch (error) {
    if (error instanceof ApiError && (error.status === 404 || error.status === 500)) {
      console.warn('[previewApi.getTeamStats] 使用 mock 数据')
      return getMockTeamStatsData()
    }
    throw error
  }
},

/**
 * 获取团队成员列表
 * 接口: GET /escort/team/members
 * 通道: escortRequest
 */
getTeamMembers: async (params?: PaginationParams): Promise<TeamMembersResponse> => {
  try {
    const query = new URLSearchParams()
    if (params?.page) query.set('page', params.page.toString())
    if (params?.pageSize) query.set('pageSize', params.pageSize.toString())
    return await escortRequest<TeamMembersResponse>(
      `/escort/team/members${query.toString() ? `?${query}` : ''}`
    )
  } catch (error) {
    if (error instanceof ApiError && (error.status === 404 || error.status === 500)) {
      console.warn('[previewApi.getTeamMembers] 使用 mock 数据')
      return getMockTeamMembersData()
    }
    throw error
  }
},

/**
 * 获取分润记录
 * 接口: GET /escort/distribution/records
 * 通道: escortRequest
 */
getDistributionRecords: async (params?: PaginationParams): Promise<DistributionRecordsResponse> => {
  try {
    const query = new URLSearchParams()
    if (params?.page) query.set('page', params.page.toString())
    if (params?.pageSize) query.set('pageSize', params.pageSize.toString())
    return await escortRequest<DistributionRecordsResponse>(
      `/escort/distribution/records${query.toString() ? `?${query}` : ''}`
    )
  } catch (error) {
    if (error instanceof ApiError && (error.status === 404 || error.status === 500)) {
      console.warn('[previewApi.getDistributionRecords] 使用 mock 数据')
      return getMockDistributionRecordsData()
    }
    throw error
  }
},

/**
 * 获取晋升状态
 * 接口: GET /escort/promotion/status
 * 通道: escortRequest
 */
getPromotionStatus: async (): Promise<PromotionStatusResponse> => {
  try {
    return await escortRequest<PromotionStatusResponse>('/escort/promotion/status')
  } catch (error) {
    if (error instanceof ApiError && (error.status === 404 || error.status === 500)) {
      console.warn('[previewApi.getPromotionStatus] 使用 mock 数据')
      return getMockPromotionStatusData()
    }
    throw error
  }
},
```

---

## 四、类型定义

### 4.1 API 响应类型

```typescript
// types.ts 新增

// ============================================================================
// 分销中心类型定义
// ============================================================================

/** 邀请码响应 */
export interface InviteCodeResponse {
  inviteCode: string
}

/** 团队统计响应 */
export interface TeamStatsResponse {
  teamSize: number           // 直属团队人数
  totalTeamSize: number      // 总团队人数（含间接下级）
  monthlyTeamOrders: number  // 本月团队订单数
  monthlyTeamRevenue: number // 本月团队收入（分）
  monthlyDistribution: number // 本月分润收入（分）
  totalDistribution: number  // 累计分润收入（分）
}

/** 团队成员 */
export interface TeamMember {
  id: string
  name: string
  phone: string
  avatar: string | null
  gender: string
  status: 'pending' | 'approved' | 'rejected' | 'suspended'
  workStatus: 'available' | 'busy' | 'offline'
  distributionLevel: number  // 1-3
  rating: number
  orderCount: number
  createdAt: string
  // 统计字段
  totalOrders: number
  totalDistributionAmount: number
  monthlyOrders: number
  monthlyDistribution: number
  // 关联信息
  level?: {
    code: string
    name: string
  }
  wallet?: {
    balance: number
    totalEarned: number
  }
}

/** 团队成员列表响应 */
export interface TeamMembersResponse {
  data: TeamMember[]
  total: number
  page: number
  pageSize: number
  totalPages: number
}

/** 分润记录 */
export interface DistributionRecord {
  id: string
  orderId: string
  sourceEscortId: string
  beneficiaryId: string
  level: number             // 分销层级 1-3
  rate: number              // 费率（百分比）
  orderAmount: number       // 订单金额（分）
  amount: number            // 分润金额（分）
  status: 'pending' | 'settled' | 'cancelled'
  createdAt: string
  settledAt: string | null
  // 关联信息
  order?: {
    id: string
    orderNo: string
    paidAmount: number
  }
  sourceEscort?: {
    id: string
    name: string
  }
}

/** 分润记录列表响应 */
export interface DistributionRecordsResponse {
  data: DistributionRecord[]
  total: number
  page: number
  pageSize: number
}

/** 晋升条件项 */
export interface PromotionRequirement {
  required: number
  current: number
}

/** 晋升状态响应 */
export interface PromotionStatusResponse {
  currentLevel: number      // 当前等级 1-3
  targetLevel?: number      // 目标等级
  canPromote: boolean       // 是否可晋升
  requirements: {
    minOrders?: PromotionRequirement
    minRating?: PromotionRequirement
    minDirectInvites?: PromotionRequirement
    minActiveMonths?: PromotionRequirement
  }
}

/** 分页参数 */
export interface PaginationParams {
  page?: number
  pageSize?: number
}
```

### 4.2 PreviewPage 类型扩展

```typescript
// types.ts 修改

export type PreviewPage =
  // 现有页面
  | 'home'
  | 'services'
  | 'service-detail'
  | 'cases'
  | 'profile'
  // 营销中心
  | 'membership'
  | 'membership-plans'
  | 'coupons'
  | 'coupons-available'
  | 'points'
  | 'points-records'
  | 'referrals'
  | 'campaigns'
  | 'campaigns-detail'
  // 陪诊员
  | 'escort-list'
  | 'escort-detail'
  // 工作台
  | 'workbench'
  | 'workbench-orders-pool'
  | 'workbench-order-detail'
  | 'workbench-earnings'
  | 'workbench-withdraw'
  | 'workbench-settings'
  // 分销中心（新增）
  | 'distribution-team'       // 我的团队
  | 'distribution-members'    // 团队成员
  | 'distribution-records'    // 分润记录
  | 'distribution-promotion'  // 晋升中心
  | 'distribution-invite'     // 邀请海报
```

---

## 五、页面 UI 设计

### 5.1 我的团队首页 (`DistributionTeamPage`)

```
┌─────────────────────────────────────┐
│ ← 我的团队                    ⚙️    │  导航栏
├─────────────────────────────────────┤
│ ┌─────────────────────────────────┐ │
│ │  邀请码: ABC123              📋 │ │  邀请码卡片
│ │  长按复制 · 分享给好友即可绑定   │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌───────────┬───────────┬─────────┐ │
│ │   直属    │   团队    │ 本月分润 │ │  数据卡片
│ │    12     │    56     │ ¥1,280  │ │
│ │   +2本月  │  +8本月   │         │ │
│ └───────────┴───────────┴─────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ 📊 累计分润收入                  │ │
│ │                                 │ │
│ │     ¥ 12,580.00                 │ │  收入统计
│ │                                 │ │
│ │ 本月: ¥1,280  昨日: ¥86         │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ 👥 团队成员                   → │ │
│ ├─────────────────────────────────┤ │
│ │ 📈 分润明细                   → │ │  快捷入口
│ ├─────────────────────────────────┤ │
│ │ 🎯 晋升中心                   → │ │
│ ├─────────────────────────────────┤ │
│ │ 📢 邀请好友                   → │ │
│ └─────────────────────────────────┘ │
│                                     │
└─────────────────────────────────────┘
```

### 5.2 团队成员列表 (`DistributionMembersPage`)

```
┌─────────────────────────────────────┐
│ ← 团队成员                  筛选 ▼  │  导航栏
├─────────────────────────────────────┤
│ 直属成员 (12人)                     │  标签页
├─────────────────────────────────────┤
│ ┌─────────────────────────────────┐ │
│ │ 👤 张三      L2·合伙人      →  │ │
│ │ 本月订单: 8   本月贡献: ¥128    │ │
│ │ 累计订单: 56  累计贡献: ¥1,280  │ │
│ ├─────────────────────────────────┤ │
│ │ 👤 李四      L3·陪诊员      →  │ │  成员列表
│ │ 本月订单: 3   本月贡献: ¥45     │ │
│ │ 累计订单: 23  累计贡献: ¥560    │ │
│ ├─────────────────────────────────┤ │
│ │ 👤 王五      L3·陪诊员      →  │ │
│ │ 本月订单: 0   本月贡献: ¥0      │ │
│ │ 累计订单: 12  累计贡献: ¥280    │ │
│ └─────────────────────────────────┘ │
│                                     │
│         ── 加载更多 ──              │
│                                     │
└─────────────────────────────────────┘
```

### 5.3 分润记录 (`DistributionRecordsPage`)

```
┌─────────────────────────────────────┐
│ ← 分润明细                  本月 ▼  │  导航栏 + 时间筛选
├─────────────────────────────────────┤
│ ┌─────────────────────────────────┐ │
│ │ 本月分润: ¥1,280.00             │ │  汇总卡片
│ │ 待结算: ¥128.00                 │ │
│ └─────────────────────────────────┘ │
├─────────────────────────────────────┤
│ 12月12日                            │  日期分组
│ ┌─────────────────────────────────┐ │
│ │ 订单分润 · 来自张三              │ │
│ │ 订单号: 202412120001            │ │
│ │ 订单金额: ¥299  费率: 5%        │ │
│ │ +¥14.95          10:30    已结算│ │
│ ├─────────────────────────────────┤ │
│ │ 订单分润 · 来自李四              │ │  记录列表
│ │ 订单号: 202412120002            │ │
│ │ 订单金额: ¥199  费率: 3%        │ │
│ │ +¥5.97           09:15    待结算│ │
│ └─────────────────────────────────┘ │
│                                     │
│ 12月11日                            │
│ ┌─────────────────────────────────┐ │
│ │ ...                             │ │
│ └─────────────────────────────────┘ │
│                                     │
└─────────────────────────────────────┘
```

### 5.4 晋升中心 (`DistributionPromotionPage`)

```
┌─────────────────────────────────────┐
│ ← 晋升中心                          │  导航栏
├─────────────────────────────────────┤
│ ┌─────────────────────────────────┐ │
│ │       当前等级                   │ │
│ │         L3                      │ │
│ │      普通陪诊员                  │ │
│ │                                 │ │
│ │   L3 ────●──── L2 ────○──── L1  │ │  等级进度
│ │   陪诊员    合伙人    高级合伙人  │ │
│ └─────────────────────────────────┘ │
│                                     │
│ 晋升至 L2 合伙人 条件：              │
│ ┌─────────────────────────────────┐ │
│ │ ✅ 完成订单数                    │ │
│ │    56/50 单                     │ │
│ ├─────────────────────────────────┤ │
│ │ ✅ 评分不低于                    │ │  条件清单
│ │    4.8/4.5 分                   │ │
│ ├─────────────────────────────────┤ │
│ │ ❌ 直接邀请人数                  │ │
│ │    8/10 人                      │ │
│ ├─────────────────────────────────┤ │
│ │ ✅ 活跃月数                      │ │
│ │    6/3 个月                     │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │         申请晋升（不满足条件）    │ │  操作按钮
│ └─────────────────────────────────┘ │
│                                     │
└─────────────────────────────────────┘
```

---

## 六、Mock 数据设计

### 6.1 团队统计 Mock

```typescript
function getMockTeamStatsData(): TeamStatsResponse {
  return {
    teamSize: 12,            // 直属 12 人
    totalTeamSize: 56,       // 总团队 56 人
    monthlyTeamOrders: 45,   // 本月团队订单 45 单
    monthlyTeamRevenue: 1350000, // 本月团队收入 13500 元
    monthlyDistribution: 128000, // 本月分润 1280 元
    totalDistribution: 1258000,  // 累计分润 12580 元
  }
}
```

### 6.2 邀请码 Mock

```typescript
function getMockInviteCodeData(): InviteCodeResponse {
  return {
    inviteCode: 'ABC123',
  }
}
```

### 6.3 团队成员 Mock

```typescript
function getMockTeamMembersData(): TeamMembersResponse {
  return {
    data: [
      {
        id: 'mock-member-1',
        name: '张三',
        phone: '138****1234',
        avatar: null,
        gender: 'male',
        status: 'approved',
        workStatus: 'available',
        distributionLevel: 2,
        rating: 4.8,
        orderCount: 56,
        createdAt: '2024-06-15T10:00:00Z',
        totalOrders: 56,
        totalDistributionAmount: 128000,
        monthlyOrders: 8,
        monthlyDistribution: 12800,
        level: { code: 'L2', name: '合伙人' },
      },
      {
        id: 'mock-member-2',
        name: '李四',
        phone: '139****5678',
        avatar: null,
        gender: 'female',
        status: 'approved',
        workStatus: 'busy',
        distributionLevel: 3,
        rating: 4.5,
        orderCount: 23,
        createdAt: '2024-09-20T14:30:00Z',
        totalOrders: 23,
        totalDistributionAmount: 56000,
        monthlyOrders: 3,
        monthlyDistribution: 4500,
        level: { code: 'L3', name: '陪诊员' },
      },
    ],
    total: 12,
    page: 1,
    pageSize: 20,
    totalPages: 1,
  }
}
```

### 6.4 分润记录 Mock

```typescript
function getMockDistributionRecordsData(): DistributionRecordsResponse {
  return {
    data: [
      {
        id: 'mock-record-1',
        orderId: 'order-001',
        sourceEscortId: 'escort-001',
        beneficiaryId: 'current-escort',
        level: 1,
        rate: 5,
        orderAmount: 29900,
        amount: 1495,
        status: 'settled',
        createdAt: '2024-12-12T10:30:00Z',
        settledAt: '2024-12-12T18:00:00Z',
        order: {
          id: 'order-001',
          orderNo: '202412120001',
          paidAmount: 29900,
        },
        sourceEscort: {
          id: 'escort-001',
          name: '张三',
        },
      },
      {
        id: 'mock-record-2',
        orderId: 'order-002',
        sourceEscortId: 'escort-002',
        beneficiaryId: 'current-escort',
        level: 1,
        rate: 3,
        orderAmount: 19900,
        amount: 597,
        status: 'pending',
        createdAt: '2024-12-12T09:15:00Z',
        settledAt: null,
        order: {
          id: 'order-002',
          orderNo: '202412120002',
          paidAmount: 19900,
        },
        sourceEscort: {
          id: 'escort-002',
          name: '李四',
        },
      },
    ],
    total: 45,
    page: 1,
    pageSize: 20,
  }
}
```

### 6.5 晋升状态 Mock

```typescript
function getMockPromotionStatusData(): PromotionStatusResponse {
  return {
    currentLevel: 3,
    targetLevel: 2,
    canPromote: false,
    requirements: {
      minOrders: { required: 50, current: 56 },
      minRating: { required: 4.5, current: 4.8 },
      minDirectInvites: { required: 10, current: 8 },
      minActiveMonths: { required: 3, current: 6 },
    },
  }
}
```

---

## 七、实施排期

### 7.1 阶段一：基础设施（第 1 天）

| 任务 | 预估工时 | 状态 |
|------|---------|------|
| 扩展 `PreviewPage` 类型（新增 5 个页面 key） | 0.5h | ❌ |
| 新增分销中心类型定义（types.ts） | 1h | ❌ |
| 新增 previewApi 分销方法（5 个 API） | 2h | ❌ |
| 新增 Mock 数据函数 | 1h | ❌ |
| 创建 `components/pages/distribution/` 目录结构 | 0.5h | ❌ |

### 7.2 阶段二：P0 页面开发（第 2-3 天）

| 任务 | 预估工时 | 状态 |
|------|---------|------|
| 创建 `DistributionTeamPage.tsx`（团队首页） | 4h | ❌ |
| 创建 `DistributionMembersPage.tsx`（成员列表） | 3h | ❌ |
| 创建 `DistributionRecordsPage.tsx`（分润记录） | 3h | ❌ |
| 更新 `index.tsx` 路由分发逻辑 | 1h | ❌ |
| 联调测试 + 修复问题 | 2h | ❌ |

### 7.3 阶段三：P1 页面开发（第 4 天）

| 任务 | 预估工时 | 状态 |
|------|---------|------|
| 创建 `DistributionPromotionPage.tsx`（晋升中心） | 4h | ❌ |
| 晋升条件进度动画效果 | 2h | ❌ |
| 联调测试 | 1h | ❌ |

### 7.4 阶段四：P2 页面 + 优化（第 5 天）

| 任务 | 预估工时 | 状态 |
|------|---------|------|
| 创建 `DistributionInvitePage.tsx`（邀请海报） | 3h | ❌ |
| 分销入口集成到工作台首页 | 2h | ❌ |
| 完善错误处理和边界情况 | 2h | ❌ |
| 文档更新 | 1h | ❌ |

---

## 八、目录结构

开发完成后的目录结构：

```
src/components/terminal-preview/
├── components/
│   └── pages/
│       ├── distribution/              # 新增
│       │   ├── index.ts               # 导出
│       │   ├── DistributionTeamPage.tsx
│       │   ├── DistributionMembersPage.tsx
│       │   ├── DistributionRecordsPage.tsx
│       │   ├── DistributionPromotionPage.tsx
│       │   └── DistributionInvitePage.tsx
│       ├── marketing/
│       ├── escort/
│       ├── workbench/
│       └── index.ts                   # 更新导出
├── api.ts                             # 新增 5 个分销 API
├── types.ts                           # 新增分销类型
└── index.tsx                          # 更新路由分发
```

---

## 九、验收标准

### 9.1 功能验收

- [ ] 我的团队首页可正确显示团队统计、邀请码
- [ ] 团队成员列表支持分页加载
- [ ] 分润记录按日期分组显示，金额格式正确
- [ ] 晋升中心显示当前等级和晋升条件进度
- [ ] 所有页面在 API 失败时降级到 Mock 数据，不白屏

### 9.2 技术验收

- [ ] TypeScript 编译无错误
- [ ] 所有 API 使用 `escortRequest` 通道
- [ ] 金额统一使用「分」计算，显示时转换为元
- [ ] 遵循现有代码规范和组件风格

### 9.3 交互验收

- [ ] 页面切换流畅，无卡顿
- [ ] 加载状态、空态、错误态处理完整
- [ ] 点击邀请码可复制到剪贴板

---

## 十、风险与依赖

### 10.1 依赖项

| 依赖 | 说明 | 状态 |
|------|------|------|
| 后端分销 API | 7 个 API 均已就绪 | ✅ 可用 |
| escortRequest 通道 | 陪诊员请求通道 | ✅ 已实现 |
| 预览器双会话机制 | 支持陪诊员身份 | ✅ 已实现 |

### 10.2 风险项

| 风险 | 影响 | 缓解措施 |
|------|------|---------|
| API 返回格式不一致 | 中 | Mock 数据兜底，待后端确认后调整 |
| 金额精度问题 | 高 | 后端已改为分计算，前端统一使用分 |
| 图片资源缺失 | 低 | 使用 placeholder 或默认头像 |

---

## 附录：快速启动

开发时可使用以下命令快速测试：

```bash
# 启动前端开发服务
pnpm dev

# 启动后端服务
cd server && pnpm start:dev

# 访问管理后台，进入陪诊员管理或工作台页面查看预览器
```

在预览器中切换到陪诊员视角后，可通过导航进入分销中心相关页面进行测试。

---

**文档版本**: v1.0  
**创建者**: AI Assistant  
**审核人**: 待定

